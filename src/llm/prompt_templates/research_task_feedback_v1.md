任务反馈采集（知识库更新的输入）：
1. 本次任务开启了知识库反馈采集。完成上述全部规定输出后，最后额外输出一个 `<task_update_feedback>...</task_update_feedback>` 块。
2. 你的反馈不会直接写入知识库：任务结束后由独立的知识更新调用汇总全部反馈、交叉验证后再落库。
$feedback_schema
本轮的采集边界：
1. 你看到的是整段内容与调查材料：hint 侧重全局性的条目线索——主播档案是否有变化、本段涉及的游戏/事件/人物是否值得建条目或补时间线。
2. 该块解析失败只会被忽略、不会重试，也不影响你的主输出；宁可少写，不要为凑数输出低把握线索。
