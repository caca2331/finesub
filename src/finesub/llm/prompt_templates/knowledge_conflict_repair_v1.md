任务说明：$task_summary

## 发生了什么

你上一轮的知识库提案已经提交。提交时你读到的是 **rev $read_rev**；在你作答期间，
另一个任务先一步写入了知识库，现在是 **rev $current_rev**。因此你的下列提案与已经
落库的内容冲突，被丢弃（其余提案已正常写入，不需要你重做）：

$dropped_rows

## 相关条目现在的样子

下面是这些条目在 **rev $current_rev** 的当前内容。**句柄（`@k` 数字）是按现在这一版
重新分配的**，与你上一轮看到的不是同一套——只能引用下面这份里出现的句柄。

$kb_entries

## 你要做什么

逐条判断被丢弃的那几条**在看到现在的内容之后是否仍然成立**：

- 别人已经写了等价的内容 → **不要重复提**，跳过。
- 你的内容是对现有内容的补充或更正，且证据仍然充分 → 基于**当前**这一版重新提一次
  （改动要挂在上面那份里的句柄上）。
- 你原来的判断被新内容推翻，或你不再有把握 → 跳过。

只处理上面列出的这几条。**不要**趁机提出与它们无关的新更新——本轮材料没有再给你一遍，
凭印象补充等于凭空捏造。如果一条都不该重做，输出一个空的提案块即可，这是完全正常的结果。

$final_reminder
