联网检索（你自己执行）：
1. 你具备联网搜索能力，本轮没有本地搜索代理为你取数——需要查证什么就自己检索。
2. 优先检索理解本段内容所必需、且你自身知识可能不足或过时的主题：游戏剧情/系统/角色名、近期事件、社区语境、直播来源信息、专名的官方或社区公认译名。
3. 检索到的事实写进 `context_pack` 时必须区分「查证到的」与「推测的」：前者在 `sources` 里给出来源，后者在对应 `note`/`uncertainties` 中标注未确认。
4. 检索不到时不要编造来源，如实写入 `uncertainties`。
