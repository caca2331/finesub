任务反馈采集（知识库更新的输入）：
1. 本次任务开启了知识库反馈采集。请在$feedback_position_clause，最后额外输出一个 `<task_update_feedback>...</task_update_feedback>` 块；除规定块外不要输出其他内容。
2. 你的反馈不会直接写入知识库：任务结束后由独立的知识更新调用汇总各窗口反馈、交叉验证后再落库。你的职责是"现场记录线索"，让证据在细节还清晰时被留住。
$feedback_schema
本窗口的采集边界：
1. hint 只写长期有效的知识线索（人物档案变化、新事件/新剧情节点、固定译名、社区梗），不要把一次性剧情细节、单次口误当成知识线索。
2. `asr_corrections` 只写可复用的误听修正模式（如"人名 X 常被听成 Y"），不要罗列本窗口的全部修正。
3. 不要提出翻译风格、prompt 或 harness 修改建议；这些不属于反馈采集范围。
4. 没有可靠反馈时，输出各字段为空数组的 JSON，不要编造。
