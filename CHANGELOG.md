# Changelog

## [Unreleased]

### 让 AI agent 帮你把现场打包好，发给开发者

新增一份任务说明(`agent-tasks/feedback-pack/`)。跟 agent 说「这次结果不对，怎么发给
开发者」或者「我想帮你们改进」,它把相关任务收成**一个 zip** 放到你桌面,把路径给你。

报问题时带上人声轨(音频往往就是答案);单纯贡献素材时完全不带音频,而且只收你精修过的
那些任务。打过的会记下来不重复;你把精修字幕改得更好之后可以再发一次。

⚠ **它不会替你发出去**——只给你一个文件,发不发、发给谁由你决定。包里有什么会念给你听
(zip 里的 `MANIFEST.txt` 是实际清单);API key 与 `.env` 一律不会进去。

### 运行日志里现在能看到每一次 API 交互

每次 LLM 调用、每次本地 agent 调用、每次联网检索,都在落盘的运行日志里留一行:哪个模型/
provider、返回什么、花了多久。以前这份日志对这一层一个字都没有,而它正是出问题时最该
发给开发者的文件。

只记状态和一句话描述,不记提示词与回答正文——那些本来就按次完整写在任务产物里。失败时
那句描述是**对方回的原话**(HTTP 状态、端点的错误正文、provider 的异常),不是我们替它拟
的措辞:裸一个 `429` 分不出「这把 key 今天用完了」和「慢一点」。

### 绑了窗口太小的模型时，开跑前就告诉你

自己配模型组时,如果里面有成员的**最大输入 < 194,000** 或**最大输出 < 64,000**,启动会告警
一次(照跑,只是窗口被切得更碎);低于 **96,000 / 32,000** 直接停下来。**都发生在识别之前**
——配置问题不该等你先跑完几十分钟 ASR 才发现。

比的是这两列本身,不是扣掉输出之后的剩余空间,所以 Claude Haiku 4.5 这类"两边都够用、只是
总量有限"的模型是放行的。出厂配置一条都不会响。

⚠ 这**不是**把本节另一条移除掉的那道 194,000 硬上限加回来:那道上限会把声明更大的模型
**压小**,这里只是数字太小时说一声,不改任何模型的窗口。

### 输入已经是纯人声时，可以跳过人声分离

`--no-separate`。适合自己分好的人声轨、录音棚干声这类输入,省掉整条链路里最重的一步。
产物不变:`<stem>-vocal.ogg` 照样在同一个位置、同样是 16 kHz 单声道,只是由源音直接转出
而不是分离出来,所以后面每一步和重跑规则都和平常一样。

⚠ **只在你确定的时候用**:该分离而没分离不会报任何错,只是识别质量整体塌掉。另外分离后的
人声轨通常还带噪,`--vad-silero-assist` 默认开着就是为了它;真正干净的输入建议同时
`--no-vad-silero-assist`。写进 `config.toml` 是 `[separator] enabled = false`。

换了这个开关要重跑的话,和换 `--separator-rate` 一样:删掉 `<stem>-vocal.*` 及其下游。

### 修复：国内镜像下模型根本装不上

走镜像下载模型时会在第一个数据块上以 `401` 失败，模型完全装不上。原因是
`huggingface_hub` 1.x 默认走 Xet 传输，而公共镜像不代理它——镜像返回的元数据仍指向官方的
Xet 服务器，随附的令牌那台服务器不认。

现在指向非官方入口时会自动关掉 Xet（普通 HTTP 下载本来就是镜像提供的东西，什么也没损失）。
你自己设过 `HF_ENDPOINT` 时同样会关，因为那个入口撞的是同一堵墙；确实有支持 Xet 的网关，
自己设 `HF_HUB_DISABLE_XET=0` 即可，我们不覆盖显式选择。

同时修掉了让这个问题变成「一步到位失败」的那一半：镜像返回 401/403 以前不算「镜像的锅」，
所以**连自动回退官方源都不会发生**。现在会回退。
### 手改知识库时，一行写错不再让整份编辑白改

`knowledge/rendered/` 下的 markdown 是可以直接改的，改完在下一次纠错运行启动时收走。以前只要有
**一行**的写法没看懂（最常见的是术语行少写了别名那一列，写成三段），**整个文件**都会被跳过——
你在同一个文件里做的其它修改一起白改，而且提示还发不出来（见下一条）。

现在分两档：

- **行级**看不懂的，那一行被**原样**挪进条目末尾的「待归类」，同文件其余改动照常落库。挪进去的
  行会提示你跑 `finesub knowledge repair`——它把这几行连同条目现状交给 AI，让它提一个归位方案；
  默认只看不做，`--execute --apply` 才落库。实测里它认出了「这行少了别名列」，把行补齐并放回
  「频道用语」。
- **整个文件**坏了（第一行不是标题、HTML 注释没关、改名撞上别的条目），仍然是那个文件被跳过、
  原样保留——结构坏了就无从知道哪一行本该是什么，改名撞车更是只有人能拍板。

`knowledge edit` 这条交互路**不变**：人就在编辑器前面，报错指着刚写的那行比默默挪走有用。
`common` 类条目为此补了一个「待归类」小节（以前只有 streamer / style 有）。


### 三个命令行入口以前会把警告吞掉

`python -m finesub.llm.correction_translation`、`python -m finesub.llm.knowledge`、
`python -m finesub.workflows.reference_ingest` 都没有绑定终端渲染器，而线程本地的默认渲染器是**静默**
的——于是这三条路上的每一句警告都被丢在地上。实测的样子：手工改坏了 `knowledge/rendered/` 下的一个
文件，纠错跑完退出码 0、一个字都没说，而那次编辑其实被整个跳过了。现在三个入口都在 `main()` 里绑
（不是在 `__main__` 块里——`python -m finesub.llm.knowledge` 走的是包的 `__main__.py`，绑在模块自己
的 `__main__` 块里根本不会执行）。渲染器写 stderr，所以把 stdout 重定向去接 dry-run 的 prompt JSON
仍然看得到警告。

### 出厂又改回以 3.7 Flash 为主力，3.8 退到它后面

3.8 Flash 在同一个标称档位上比 3.7 多想约 1.5 倍（跨批观测：同一条流程换代前后，纠错单窗思考量
中位数 19,361 → 37,282；两批素材不同，所以这个倍数是量级而非精确值）。思考与字幕正文共用同一份
65,536 的输出上限，而没有谁拿得出对应的质量收益，所以出厂顺序反过来了：**每个 3.7 与 3.8 同时出现的模型组里，
3.7 都排在 3.8 前面**，两个 agy 预设的 Gemini 前端也换回 3.7（新增 `local-agy-gemini-3_7-flash`
一行与它的 media / native 两个 target）。

- **3.8 仍然指得到**：`--llm-model gemini-free-3_8-flash`、`local-agy-native-gemini-3_8-flash`
  等等照旧可用，只是不再自动排在前面。
- **3.8 的 thinking 映射封顶成 `medium,medium,low`**：抽象 high 被压回 medium。medium 那一格
  **故意保持 medium**——把它一并降到 `low` 的那版做过五对配对实测（同素材、同模型、同检索，
  唯一变量是映射）：纠错单窗思考量中位数 37,730 → 0，五个窗里有四个一个思考 token 都不出。
  `low` 是把要不要想交给模型，不是少想一点。
- 自建端点的 `thinking` 列因此多了第二种用法：除了「我的端点用别的词」，也可以用来给爱想的
  模型封顶。见《模型与路由》的 `thinking` 列。


### 托管运行时的三份资产与版本号搬了家（给下游维护者，不影响使用）

> 只有从源码构建、或要 fork 桌面端的人需要读这条。装好的 `finesub` 行为没有任何变化。

`desktop/` 会在 0.5.0 从本仓移出。为了让那之前的快照把边界划对，**命令行也在用的四份
文件先搬到了不属于桌面端的位置**：

| 原位置 | 现位置 |
| --- | --- |
| `desktop/VERSION` | 仓库根 `VERSION`（根 `pyproject.toml` 现在 `dynamic` 读它，版本号从两份变一份） |
| `desktop/runtime/pylock.win-py312.toml` | `src/finesub_bootstrap/pylock.win-py312.toml` |
| `desktop/runtime/pylock.win-py312.cn.toml` | `src/finesub_bootstrap/pylock.win-py312.cn.toml` |
| `desktop/resources/runtime-manifest.json` | `src/finesub_bootstrap/runtime-manifest.json` |

⚠ **对 fork 桌面端的人**：从这一版起 `desktop/` 不再自带 pylock 与 runtime-manifest，
要么消费 `finesub_bootstrap` 里的那一份，要么在自己的仓库里另存一份副本。cn 锁的生成器
也跟着锁走了：`python -m desktop.scripts.make_cn_lock` → `python -m scripts.make_cn_lock`。

### 知识库的命令，装好的版本上也能直接敲了

以前手册里那些 `python -m finesub.llm.knowledge …` 只有从仓库源码跑才用得上——装好的
`finesub` 认不出 `knowledge` 这个词，会把它当成一个文件名丢给管线。功能一直在包里，缺的
只是入口。现在三条都通了，子命令与参数和原来完全一样：

- `finesub knowledge <子命令>`——看、改、新建、退场、让 AI 蒸馏一份资料（`show` / `log` /
  `edit` / `new` / `retire` / `ingest` …）
- `finesub knowledge-share <子命令>`——和别人共用一个知识库（`mark` / `push` / `pull` …）
- `finesub knowledge-update <最终 SRT> --refined-srt <你改好的>`——**把精修好的字幕回喂**，
  不重跑管线。这一条手册以前根本没讲，现在补上了（`docs/manual/knowledge.md`）

### 纠错时整窗字幕被丢光的输出会被打回重做

模型（或替你干活的 agent）如果没真正读到窗口正文，可能交回一份「只留一条字幕、其余全部
标为丢弃」的答卷——它格式完全合法，于是一路走到成品，那一段字幕就只剩一行。现在丢弃超过
半窗的答卷会被当场打回重做。

正常丢弃不受影响：翻遍历史产物，丢得最多的一次是 21.9%（那是一份歌回，整段演唱本来就该
丢），门槛比它还高两倍有余。窗口因输出超限被拆开重试之后，拆出来的半窗不受这条限制——同样
翻历史产物，半窗本来就可能有 43.8% 是演唱。

### 窗口按模型真实的上下文切了，不再被一道 194,000 的硬上限压平

以前无论 catalog 里写多大，每个模型的输入包络都是 194,000——harness 自己有一道上限，凡是
声明值高于它的模型都被压到这个数。现在包络由模型自己的三个数算出来。

catalog 新增一列 **`context_window`**：

- **留空 = 这一行不声明联合约束**，包络就是 `max_input_tokens`。Gemini 分别公布两个上限，
  所以**免费档留空**；付费档与 agy 仍按单池填（见下表，包络 983040）。
- **单池供应商要填**（Codex、Anthropic、DeepSeek：答案和提示词花同一份预算），包络变成
  `min(max_input_tokens, context_window − 输出上限)`。
- 填得比任一半还小**直接报错**，不会被悄悄夹住。

对**默认流程（免费 Gemini）没有任何变化**：它留空，包络仍是 194,000。变化的是别的后端：

| 后端 | 输入包络 | 以前 |
| --- | ---: | ---: |
| Claude Code Opus 5 / Sonnet 5 | 872,000 | 194,000 |
| DeepSeek v4 flash / pro | 744,000 | 194,000 |
| Gemini 付费 API 与 agy | 983,040 | 194,000 |
| Codex Luna / Terra / Sol | 206,464 | 194,000 |
| **Claude Code Haiku 4.5** | **136,000** | 194,000 |

⚠ **Haiku 4.5 变小是修正**：它的 200K 是输入输出共用的池子，以前那个 194,000 并不代表它能
在装下 194,000 输入的同时还留出 64,000 给答案。

**多数情况下窗口不会因此变大**：一窗能放多少字幕由质量护栏 `[chunking]
max_window_subtitle_tokens`（默认 10000）决定，不由输入包络决定。真正受益的是**媒体很重、
文字很少**的素材——典型是高清视频纠错——那里窗口会变少（总媒体量不变，重叠段少发几遍）。
把护栏设成 0 的话就没有这层保护了，窗口大小会随模型输出上限走。

同时删掉了两个内部字段：`context_limit` 检查（新包络下 `输入 + 预期输出 ≤ 上下文`
按构造成立，它永远不会响）与 `safety_margin`。产物里的 `budget.total_with_margin` 字段
随之消失，读它的旧产物需要重跑。

设计与验收记录：`docs/plans/model-window-limits-plan.md`。

### Codex 的 GPT-5.6 Terra 可以指了

Codex CLI 除 luna、sol 外还提供 `gpt-5.6-terra`，但打包目录里只记了另外两个，于是没有目标
可指。现在 catalog 有它的行，并配了 `local-codex-completion-gpt-5_6-terra` 与
`local-codex-native-gpt-5_6-terra` 两个目标（`LOCAL_CODEX` 的行不会自动生成目标，得像
luna、sol 一样显式写）。

**默认什么都没变**——它不在任何出厂模型组里，要用得在 `config.toml` 里点名。

⚠ **`quality_score = 82` 没有实测**。它是 owner 定的位置：**在 Sonnet 5（77）与 Opus 5（88）
之间**，同时也落在自家兄弟 luna（70）与 sol（90）之间。后一个区间有厂商依据——目录里
`priority` 是 sol 6 / terra 7 / luna 8，描述依次是「reliable agentic workhorse」/「**balanced**
agentic coding model」/「fast and affordable」——但那只说明**次序**，具体数值仍是拍的。
实测过请改 `model_catalog.psv`。

其余能力列不是抄来的，是照 Codex 本机模型目录核对的：三个模型的上下文窗口、模态与是否支持
搜索工具完全相同。thinking 阶梯取 luna 那一档（`xhigh,high,medium`），因为 terra 自己的
默认推理档也是 `medium`，与 luna 同、与 sol 的 `low` 不同。
### 文档：新增《跑完之后》《调参》《出问题时》三章

以前只有 `--help`（英文）讲得清的那些东西，现在有中文的落点：

- **《跑完之后》**——`out/<名字>/` 里每个文件是什么、`--stage` 六个值各停在哪一步、
  `-annotated.csv` 九列怎么读（按 `conf` 筛出模型自己拿不准的行来人工核对），以及想重跑
  某一步该删哪些产物。
- **《调参》**——字幕太长/太碎怎么调（`--split-length-scale`，也可以写进 `config.toml`），
  识别侧与 LLM 侧各一张旋钮表，每一条都写了默认值、代价和「改完要删什么才生效」。
  `config.toml` 的 `[vad]` 一节此前从模板到文档都没提过，现在两边都有了。
- **《出问题时》**——按症状找那一页的总目录：回退 CPU、显存告警、空字幕、429、
  `config.toml` 没生效……末尾列了提 issue 该带哪几样东西。

`README.md` 的文档一节、`docs/README.md` 的地图都已指向它们。桌面端 README 另加了一节
「界面能设什么」：界面上的每一项对应哪个命令行选项、哪些只有命令行才有，以及**三处默认值
和命令行不一样**（知识库、LLM 看什么、跑完清不清中间产物）。

### 修复：太老的显卡上，识别不再被交给显卡然后报错

「这张卡能不能用」问 CTranslate2 时，问的是「驱动报得出显卡吗」——那是 `cudaGetDeviceCount`，
它数的是设备，不是这块卡有没有对应的核函数。于是一张 GTX 10 系的卡答「有一个」，识别拿着它
去跑，在第一次编码时死于 `no kernel image is available for execution on the device`。文档
承诺的是自动回退 CPU，实际得到的是任务失败。

现在多问一句算力下界（补丁版 CTranslate2 编译到 7.0）：低于它就**回退 CPU 并说明原因**，
和显卡不被支持时的其余阶段一致。

⚠ **比编译范围更新的卡不受影响**：驱动会即时编译，今天的 RTX 50 系正是这么跑的。挡住的只有
比下界更老的卡。

顺带修掉一条过时的提示：CTranslate2 装成没有矩阵后端的版本时，报错里建议的
`--asr-backend wt` 早已随单文件 WT 路径一起删除；现在指向《patched CTranslate2》的安装与
自检说明。

### 文档：显卡回退的说法改准确了

「不支持的显卡自动回退 CPU」这句话对结果是对的，对过程不准：识别用 CTranslate2、人声分离与
第二模型校验用 PyTorch，两者支持列表不同，所以一张 PyTorch 认不出的卡上，识别**可能仍在显卡
上跑**（快十倍，那是好事）。`README.md` 与《资源与大文件》都写清了这一点。
### 一个文件一个进程地跑时，用尽的 agent 订阅现在也会被冻结

本地 agent CLI（codex / claude / agy）没有查额度的命令，所以「订阅用完了」只能从失败里推断：
同一订阅连续失败两次，就发一次极小的探测，探测也失败才冻结两小时。冻结本来就存在 `.state`
里，**但导向冻结的那两次失败只记在内存**——于是一次命令跑一个文件的用法（对每个文件调一次
`python -m finesub.llm.correction_translation`）永远攒不到第二次：每个进程失败一次就退出。
结果是对着一个已经用完的订阅，每个文件仍然照常启动一次 CLI。现在这个计数也落盘了。

顺带：连续失败的证据四小时内有效（比冻结长，这样解冻后的第一次失败会直接进探测，而不是从头
再数两次）。成功照旧立即解冻；计数则在探测成功、或记过失败的那个进程自己调用成功时清零——别的进程留下的计数最多让下一次失败早一步发探测，那次探测成功就把它清掉。

### 「没有可用 API key」的报错现在说清是哪一种等待

以前一律是 `All API keys for <tier> are daily-exhausted or in cooldown.`，把两件相差数小时的
事并成一句：日额度用尽要等配额自己重置，而短时冷却只有二十分钟。现在按实际原因分开报，冷却
还会给出最早可重试的时刻，例如
`No API key for GEMINI_FREE could be tried: 2 in a transient cooldown after recent errors, retryable from 09:43 UTC.`
### 修复：自带的 ffmpeg 缺 `libx264`，纠错一用到剪辑就报错

自动预置的 ffmpeg 钉的是 BtbN 的 **lgpl** 构建，而它配了 `--disable-libx264`；引擎给 LLM
纠错切的每一份剪辑都用 `libx264` 编码——视频窗、agy 的转码，以及把音频窗封成单黑帧 MP4 的
那一步（agy 不收裸音频 MIME）。结果是：**只要一次运行的纠错参考不是纯文本，就会报
`Unknown encoder 'libx264'`**，而且是在纠错阶段才炸，前面的分离与识别已经白跑。

- 清单改钉同一个 release 的 **gpl** 构建。**已经装了 lgpl 的机器会自己换掉**（目录名变了，
  预置时读作缺失），不需要手动清理。
- 复用系统 ffmpeg 的体检也补上了：以前它查的是 `libopus`（我们从来没用过——`.ogg` 是
  soundfile 写的）和 `aac`，唯独不查 `libx264`，所以一个缺 x264 的构建能一路通过。现在查
  `aac` + `libx264`。⚠ **如果你的系统 ffmpeg 没有 libx264，它现在会被判为不可用**，安装器会
  改为下载托管的那份；不想下载就换一个带 libx264 的构建。
- 源码安装自备 ffmpeg 的同理，README 与仓库安装文档已注明。

### 资源启用失败时，报错会说是什么占着目录

安装的最后一步是把解压好的目录改名到位，Windows 在名字被占用时会拒绝。有界重试早就有了，
但**等不到的时候**界面上只剩一行 `[WinError 5] 拒绝访问`——不说是哪个资源，也不说能做什么。
现在会点名资源并列出常见的占用者（杀毒软件、网盘同步、停在该目录的资源管理器/终端），原始
错误仍附在后面。另外，失败后的清理如果也被同一个句柄挡住，不再用它的错误盖掉真正的原因。

### Gemini 3.8 Flash 上线，agy 那一档整体换过去

- **API 侧**：免费档与付费档各加了一行 3.8 Flash，排在同族 3.7 之前。纠错、research 和
  knowledge 现在先试 3.8，失败再依次退回原来的名单——原有顺序一格没动，只是在 3.7 前面
  插了它的新版。
  ⚠ **它的免费档限额（5 RPM / 250k TPM / 20 RPD）是沿用 3.7 的，没有单独核过**——上游
  没有公开这几个数。限流器直接读它们：如果实际额度更低，你会更早撞到 429（管线会退到
  名单里的下一个，不会失败）。核实之后请改 `model_catalog.psv`。
- **Antigravity（agy）侧**：那一档的 Gemini 前端从 3.7 Flash **换成** 3.8 Flash（不是并列）。
  `agy` 预设的 `--test-profile` 目标也跟着换。
  ⚠ **这一步删掉的 3.7 id 后来又加回来了**，所以**本版不需要你改配置**：
  `local-agy-gemini-3_7-flash`（catalog 行）与 `local-agy-media-gemini-3_7-flash` /
  `local-agy-native-gemini-3_7-flash`（两个 target）在同一个开发周期里随「出厂又改回以
  3.7 Flash 为主力」一条重新登记，现在两组 id 都解析得到。

### 老显卡上，识别现在还能用显卡跑

判断「能不能用显卡」以前统一问 PyTorch。但识别用的是 CTranslate2，两者支持的显卡范围不
一样：一张 PyTorch 认不出的老卡，CTranslate2 可能照样跑得动。以前这种机器的识别会白白退到
CPU（慢十倍），现在它会用显卡。

这类机器上，人声分离和第二模型校验仍然走 CPU——它们确实是 PyTorch 的活。

⚠ **本版后来给这条加了下界**：比补丁版 CTranslate2 的编译范围（算力 7.0）更老的卡
——例如 GTX 10 系——仍然回退 CPU，见本节「太老的显卡上，识别不再被交给显卡然后报错」。
这里说的「老显卡」指的是那条线以上、只有 PyTorch 认不出的卡。

### CTranslate2 装坏时不再「跑完但字幕是空的」

识别用的是 CTranslate2，不是 PyTorch，而以前判断「能不能用显卡」问的是 PyTorch。
装了 CPU-only 的 CT2 却有显卡时，会把显卡交给它，然后失败。

**更糟的是那次失败不会让任务停下来**：识别是一段一段做的，一段失败就记一条警告、跳过、
继续下一段。于是每一段都用同样的方式失败，任务「成功」结束，交出一个**空字幕文件**。

现在识别开始前会先问 CT2 自己：不行就**退回 CPU 并说明原因**（慢，但字幕是对的）；
如果它连 CPU 也没有可用的后端（安装或构建有问题），**任务直接报错**并指向 CT2 的安装
说明，而不是安静地产出空文件。

⚠ 报错的位置是**识别阶段开始时**——人声分离和语音检测已经跑过了。想更早发现，需要在
分离之前也装一次识别模型，而那和「分离器与识别模型不同时占显存」是冲突的。

### `--device cpu` 现在让整条任务都离开显卡；桌面「自动」不再把第二模型校验锁在 CPU

- **`--device cpu` 以前只管识别**：人声分离仍按显卡档位自己决定用不用卡，所以
  `--gpu-tier standard --device cpu` 会先在显卡上做分离——正是你想让出去的那张卡。现在这个
  请求传到分离器，三个阶段（分离 / 识别 / 第二模型校验）都不碰 CUDA，且不会再对一张好卡
  打「CUDA 不可用」的假警告。
- **桌面端处理设备保持「自动」时**，如果识别因为 CTranslate2 用不了显卡而退到 CPU，第二模型
  校验会被误当成「用户要求 CPU」而跟着留在 CPU——哪怕显卡空着、PyTorch 用得好好的。现在
  「没选」就是「没选」，校验照常拿到显卡。

### 没有显卡的机器现在是 `cpu` 档，不再冒充「入门显卡档」

以前一台没有显卡的机器会被归到 `entry`（基础 GPU 加速）档，于是它的运行记录里写着
「显存预算 3GB」——一份它从来没有的预算。现在这种机器落在新的 `cpu` 档，记录如实。

也可以手填 `--gpu-tier cpu`，意思是「这次把整张卡让给别的程序」。⚠ 它和 `--device cpu`
不是同一件事的两种写法：**`--gpu-tier cpu --device cuda` 会直接报错**，因为那两句话没法
同时成立。反过来「显卡档 + `--device cpu`」是正常的。

⚠ 一张老到当前 PyTorch 认不出的显卡**仍然算显卡档**（落 `entry`），不会被归到 `cpu`：
识别用的 CTranslate2 有自己的一套支持列表，可能还跑得动它。

### 新增显卡档位 `standard_large_vram`（12GB 起的卡自动选中）

以前 12–16GB 的卡只能二选一：`standard` 的两个分离 worker 但显存预算限死在 6.5GB，
或者 `high` 的 10GB 预算却附带一个**实测更慢**的第三个 worker。

新档位把这两件事拆开：**worker 数与 `standard` 相同（2 个），显存预算与 `high` 相同
（10GB）**。多出来的显存只有一个花家——第二模型校验的快路（要求主模型之外还有 3.5GB
空闲）。换成 `large-v3` 或日语微调那种大模型时，`standard` 不够、这一档够。

`auto` 现在在 12GB 起的卡上直接选它；`high` 不变，仍然只在卡报 ≥24GB 时放行。

### 跑 large-v3 级模型时，加 `--asr-decode-batch 8` 能快 1.4–1.6 倍

这个选项默认还是关的——在默认模型上它只值 6%，不够本。但在 `large-v3` 和日语微调这类
大模型上实测能快 **1.4 到 1.6 倍**，字幕内容的差异在噪声量级。所以用大模型时值得加上它。

代价看你的显卡：8–11GB 的卡（`standard` 档）显存装不下两个模型，第二模型校验会退到 CPU；
12GB 起的卡自动落在新的 `standard_large_vram` 档，显存够，校验照常在显卡上跑。

顺带修了一个数：`large-v3` 的常驻显存一直记着 6.15 GB，那其实是**批量解码时**的数字，
单条解码实测只有 3.89 GB。这让它在中配档上也能和第二模型共用显卡了。

### ASR 多了一个日语备选模型（实测与默认打平）

`--model TransWithAI/whisper-ja-1.5B-ct2` 现在是一个正式列出的选项：Whisper large-v3 的
日语微调，约 3.1 GB，**只有你点名它才会下载**。它享受和默认权重一样的镜像路由与摘要校验。

**在 11 份日语素材上和默认模型比过**：从两者不一致的地方随机抽 200 条做盲听审计，
判决 64 : 70。折回全部字幕行，微调更准的约 7%、默认更准的约 6%，**净额只有 1–3 个
百分点**。它确实很少「整段复读」、漏字也更少，但代价是慢 2.4 倍、显存多一倍。所以它是
一个**给特定毛病用的备选**，不是升级。

### 新增《模型选择》一章

[`docs/manual/models.md`](docs/manual/models.md)：ASR 三个可选模型各自什么时候用（默认的
`large-v3-turbo`、只作开发对照的 `large-v3`、未实测的日语微调）、不可更换的人声分离与
第二模型是什么，以及各家 LLM 后端的使用印象。

### agent CLI 版本太旧会提示一句（但不会拦住你）

用到本地 agent（codex / claude / agy / dsh）时，如果你装的那个 CLI 比我们验证过的版本更旧，
现在会出现一条提示，告诉你装的是哪个版本、我们验证到哪个版本。**只是提示**——调用照常进行，
不会因此少一个可用模型。当前验证到的版本：codex `0.147.0`、claude `2.1.231`、agy `1.1.24`、
dsh `0.1.1-rc.2`。

如果哪天某个 CLI 改了 `--version` 的输出格式，导致我们读不出版本号，也会提示一句——
免得这个检查悄悄失效却看起来一切正常。

### dsh 现在只放行核对过的插件

以前我们是**按名字逐个关掉**不想让模型用的 dsh 插件（dsh 没有「只允许这几个」的开关）。
这意味着你自己往 `$DSH_HOME` 装的插件、或者 dsh 升级新带的插件，我们既没关也没看过，
它们会直接出现在模型面前。

现在每次用 dsh 之前会先问它「你这个 profile 由哪些插件组成」，凡是我们没核对过的，
既**提示**（并说明是你装的还是 dsh 自带的），也**在这次调用里关掉**——相当于反过来变成了
白名单。实测：往 dsh 配置里加一个真插件，不开这条策略时模型确实多出它的工具，开了就没有。

同时把用不到的也关了。出厂 dsh 给模型 11 个工具，现在 5 个——多出来的那些（另一个文件
编辑器、磁盘搜索、几个子 agent 遥控工具）一个都用不上：窗口内容是直接按路径交给它的，
不需要它自己翻磁盘。每一条都是让模型报了工具清单、确认关掉后少了哪个、且照样能答题才关的。

四点要知道：

- **万一 dsh 答不出它由哪些插件组成，这次就不走 dsh 了**，直接换名单里的下一个模型，
  并说明原因。因为清单拿不到就没法保证「只放行核对过的」，与其冒险跑，不如换一个。
  确实想在这种情况下继续用 dsh，把 `deny_unknown_plugins` 设成 False。
- 万一哪天 dsh 升级新带的插件是它自己运行需要的，这条策略会把它也关掉、dsh 会明显报错。
  这是故意的（比悄悄改变行为好找），真遇到时可以先把这条策略关掉应急。
- **如果你把 dsh 配置放在自定义的 `DSH_HOME`，现在会被用上了。** 以前这个环境变量会被我们
  滤掉，结果是我们的调用读默认的 `~/.dsh`，你自己敲 `dsh` 读的却是另一处。
- 有 dsh 任务跑到一半没跑完的话，这个改动会让它从上一个未完成的窗口重跑。
### 一个窗口最多拆一次，之后任务停下来报错

一句字幕太长、模型答不完时，程序会把那个窗口对半拆开重试。以前这个拆分没有上限：真正卡住的
窗口会被反复对半、一路花掉配额，最后仍然失败。现在**最多拆一次**，半窗还不成就停下这个任务
并说清楚是哪个窗口——不再拿配额换同一个失败。

产物里的窗口编号因此只会出现 `0001-a` / `0001-b` 两种后缀，不再有 `0001-a-a`。断点续跑
也一样受这条限制（以前续跑时重新适配窗口的那条路没有上限，能绕过它）；但**之前已经跑完并
记录下来的窗口不受影响**，照常复用——已完成的产物不会因为今天换了个参数就作废。

### 可以让字幕沿用一套固定的翻译口味了

一家字幕组、一位译者的习惯 —— 引号怎么标、自称怎么转、语气松还是紧 —— 现在是知识库里的一个
条目，每条约定是里面的一行：

```powershell
python -m finesub.llm.knowledge new style 某字幕组 --intro "某字幕组的中文字幕口味"
python -m finesub.llm.knowledge edit 某字幕组     # 在「约定」节里一行写一条
finesub 视频.mp4 --style 某字幕组
```

也可以写进 `config.toml` 当默认（`[llm] style = "某字幕组"`），多套用逗号分隔。这套约定会进
纠错/翻译的提示词；跑完之后，如果这次带了人工精修字幕（`--refined-srt` 配
`--knowledge update`），AI 还会把这次看到的**成类做法**提回同一个条目——它只动你点名的那一
套，而且没有精修对照时不会动（否则就是它把自己的习惯学回去）。

每节最多 20 行、每行 200 字，整条还有一个约 6000 token 的总量上限。写满了要加新的就得先改掉
或删掉一条：给模型看的篇幅有限，逼着你留下真正值得留的。超出总量时这一次只能压缩（改短、
合并或删几条），不会因此卡住不让你改。举例时给约定和例子用同一个方括号短名配对，指向不存在
的约定会被拒绝。

风格常和它服务的主播/作品同名，这是允许的；同名时命令行要写成 `style/某字幕组`
（`show`、`edit`、`retire`、`ingest`、`share push` 都一样），不写会列出有哪几个同名的，
**不会替你猜**。

⚠ 中途改 `--style` 不会让已经翻好的部分重跑，前半段会保持原来的口吻。要整片统一就重跑一遍。

**不写 `--style` 时**会去用一套叫 `default_style` 的风格——你自己建过才有，没有就当没这
回事（不会报错）。想让它一直生效，就把常用约定写进这个名字下面。用
`--llm-difficulty efficiency`（最省的一档）时默认不用风格，要用就明写 `--style-mode read`。

原先那两个 markdown 台账（`常见翻译错误` 与 `翻译范例`）**退役了**：不再注入提示词，AI 也
不再往里写。它们的内容该不该变成一套 style，由你自己决定——`knowledge ingest` 可以把任何
一份材料蒸馏进一个条目。

### 知识库：散文行里可以写竖线了

以前不管哪一节，一行里出现三个及以上竖线会被当成术语行、恰好两个会直接报格式错误。字幕的
多人行本来就是用 ` | ` 连起来的，所以这类句子写不进条目。现在**只收散文的小节里竖线就是普通
字符**，术语行的那两条规则只在允许术语行的小节里生效（比如 streamer 的「频道用语」）。

### `--asr-decode-batch` 现在真的会组批（但默认仍关）

这个选项以前只是被记录、不起作用。现在设成 4 会让识别把接下来几段一起解码。实测在 12 份
素材上只快约 6%，而且批越大越容易改动个别字（16 时明显），所以默认值还是 1；除非你自己
想试，不用动它。

### 第二模型校验（Qwen 裁判）更快了，字幕内容不变

一条素材跑完 Whisper 之后，会有一个小模型再听一遍可疑片段。以前它一条一条听，每条约
半秒到一秒，加上约 3 秒的模型加载；现在它把片段**成批**听（16 条 12.7 秒→1.5 秒），并且
在**主识别还在跑的时候**就把模型装好（显存足够的档位；4 GB 显卡照旧等主识别结束）。
一条 8 分钟素材的这一步从 4.8 秒降到 0.9 秒。单次要听的音频很多时（约两分半以上，比如
缺口很长或将来整段复核），还会自动切到一条编译过的快路：首次多等约半分钟编译，之后每分钟
素材只要 0.7 秒；显存不够的档位不走这条路。

听到的内容偶尔会有一两个字不同（同一段音频两种算法给出不同的近似读法），但决定「删不删
这条字幕」的判断在 31 个案例里 31 个一致。想关掉那条快路：环境变量 `FINESUB_REFEREE_ACCEL=0`。

### 同一个标题写了两份文件时，不再变成两个同名词条

旧知识库里可能有两个文件写的是同一个条目（比如文件名一个用「の」一个用「ノ」，标题却一模一样）。
迁移过去会得到两个同名词条，按名字找只会找到其中一个，另一个的内容就此失联——而且**没有任何提示**。

现在会挑一份作为正主（**改动时间较新的那份；时间一样就取内容较多的**），另一份的内容整段并进
正主的「待归类」小节，并让它自己退场。**一个字都不丢**，只是需要你或 agent 后续看一眼那些行
该归到哪。迁移报告里会写明挑了哪份、并了哪份。

### 让 agent 替你做的几件事，改成它自己去找

原先那几份任务说明依赖 Claude Code 的「skill」机制自动注入。现在它们搬到了仓库根的
`agent-tasks/`，任何能读文件的 AI agent 都能自己找到、自己读——不再绑定某一个工具，也不需要
安装或配置。你只要说「审一下这次 run」「把这份笔记加进知识库」这类话就行。

清单与用法见 [`docs/manual/agent-tasks.md`](docs/manual/agent-tasks.md)。


### 升级不再因为「格式不合」打不开知识库

拿一位用户的真实知识库实测 0.4.2→0.5.0 的一次性迁移，撞上两个硬阻塞：词条里有自定义小节
（比如「翻译约定」）时导入直接报错中止；以及导入后的逐字节校验不过就删库中止——而校验之所以
不过，是因为我们**故意**丢掉了整行 HTML 注释（那本是给我们自己的说明注释用的规则），
外加小节末尾多写的空行没被记下来。两件都是我们自己的规则撞上了别人的写法。

现在导入器只做档案员：读不懂的小节**原样收下**（保留原小节名），用户自己写的注释和空行
一并保留，所以逐字节校验照旧全强度通过。读不懂的内容随后被自动归到「待归类」，交给模型去
判断该怎么归位——**不再靠给每种写法打补丁去硬凑格式**。

### 新命令：把一份材料蒸馏进知识库

`python -m finesub.llm.knowledge ingest --subject <条目> --material <文件>`：你自己写的主播
习惯笔记、存下来的网页、别处整理的术语表，都可以交给模型蒸馏成对某个条目的修改提案。
`--prompt` 可以加一句交代（「重点收自称与观众叫法」）来偏向它收什么。和其他 LLM 入口一样
三档：默认只渲染提示词不联网，`--execute` 才跑，`--apply` 才写库。

收录标准写进了共用的判断标准：知识库只为**让 ASR 听对**和**让字幕写对**两件事服务，别的
不收。⚠ 从材料吸收时手上没有本次的转写文本作对照，所以这一轮**不记误听**——那要留给跑完
一次纠错、有精修对照的那一轮。

（原先塞在 `repair --material` 里的同一能力搬到了这里；`repair` 现在只处理扫描出的候选。）

### 人工维护命令的实现改名

`python -m finesub.llm.knowledge` 这条命令没变，改的是它的实现位置：
`knowledge/node/cli.py` → `knowledge/maintain.py`。原来的 `node.cli` 这个叫法容易被误读。


### 共享库拉取的合并冲突不再打完就没

`share pull` 遇到本地与远端都改过同一个字段时，一直是**保留本地、丢弃远端、打印一行**。
那一行滚出屏幕就没了，而且因为远端值从不落库，**下一次 pull 会把同一条冲突再报一遍**——
既没人记得处理过没有，也没有任何地方能回去看当时两边各是什么。

现在每条未决字段写进知识库根的 `share-conflicts.jsonl`（两边的值、共同基线、是哪个条目的哪个
字段、哪次 pull 看到的），并新增 `share conflicts` 列出未决项。`--repair` 把它们连同条目**当前**
的内容交给模型重判，三档与其他 LLM 入口一致：默认只渲染 prompt，`--execute` 才跑会话，
再加 `--apply` 才落库，且仍走和其他写入完全相同的校验→应用路径。

两种裁定的粘性不同，因为冲突会被反复上报：「本地是对的」会粘住，之后的 pull 不再重开；
「我改掉了」不粘——同一条再出现就说明没改成，会重新打开。模型说「改好了」也不算数：
字段没真的变化时那条保持未决。

### 修复：人声分离跑完不把显存还回来

`run_vocal_separation` 返回后，**一整份 fp32 Roformer 权重（676 个张量，约 0.60 GiB）
仍留在显存里**——pool 的 master 已置 None、租约归零、`gc.collect()`、`empty_cache()`
乃至 `torch.compiler.reset()` 全做过都没用，`audio-separator` 下游还有一条看不见的引用
吊着那个 module。

**是按调用漏，不是按 worker。** 批量是同进程线程，所以每个文件漏一次：十个文件六 GiB，
而 `entry` 档声称只要 3GB。

**修法走过一次弯路。** 第一版是 `to("cpu")`——把权重搬下 GPU。显存读数确实好看了
（0.615 → 0.009 GiB），但那条隐藏引用在主机上一样吊着它：四次连跑实测，活着的 CPU 张量
每次精确 **+0.595 GiB**，RSS 跟着涨。**显存泄漏被搬成了内存泄漏**，而当时的回归测试只看
`torch.cuda.memory_allocated()`，看不见。评审抓到了这一点。

现行修法是**释放存储而不是搬家**：把每个 parameter/buffer 的 data 换成空张量。谁还握着这个
module，拿到的是一个参数为空的壳——无害，因为没人复用它（pool 已丢掉 `_master`，下一次
acquire 从 checkpoint 重建）。四次连跑实测：CUDA 残留 0.009–0.010 GiB，**CPU 张量全 0.000**，
RSS 从第二次起持平（第一次那 1.2 GiB 是库与模型机制的一次性加载）。

它还牵出两件事：

- **VAD-ASR 阶段的显存预算此前一直在量错东西。** 那一栏的 `peak_gmem`（0.87 / 1.55 /
  2.22 GiB）主要是这份残留；泄漏一修就只剩 0.22–0.26 GiB。
- 顺带确认 **CT2 的分配对 torch 计数器不可见**，所以 Whisper 解码器本来也没被那个检查覆盖。
  重资源测试里那条「防止空洞通过」的守卫（`peak_gmem > 256MB`）因此是在验一件它验不到的事，
  **它一直能过只是因为量到了泄漏**。现在改成从产物验：没有 segments 就是解码器没跑。

新增重资源回归 `test_the_separator_gives_its_weights_back`：连跑**三次**分离，
CUDA 与**主机侧** torch 张量残留都必须低于 0.15 GiB 且**不随次数放宽**——一次看得出泄漏大小，
两次看得出是否累加，三次才分得清「有界的一次性开销」和「无限增长」。同时断言两侧，
是因为只看显存正好漏掉上面那种修法。

**顺带发现那条重资源测试的 ASR 那一半一直是空跑的**：合成素材是包络下的纯音，能骗过能量
VAD，但解码器转不出任何东西——三个档位实测 segments 全为 0。它此前"通过"，靠的正是被泄漏
的权重喂饱的那个守卫。现在守卫改成从产物验，而 ASR 那一半改用真实语音
（`assets/harvard.flac`，`assets/` 不进仓库，缺失时明确 skip 并说明）；
分离器那一半照常跑，它一直是真的。

### 修复：Python 3.10 会静默忽略配置文件里的联网 opt-out

上一轮把 `tomllib` 改成惰性导入时，`except ImportError: return False` 等于「读不到配置就当
没禁用」——于是 3.10 用户即使照 `docs/manual/resources.md` 写了 `[cli] update_check = false`，
CLI 仍然会去访问 PyPI。一个文档明确承诺过的开关被静默忽略，而且恰好是在联网这条轴上。

两处改动：wheel 为 3.10 声明 `tomli` backport，让这个设置在所有支持的版本上都真的生效；
同时把无法解析时的默认从「未禁用」翻成**「按禁用处理」**——读不到就说明我们无法确认用户
是否要求过别联网，那就不联。代价是万一 backport 也缺席，3.10 用户会少一条提醒，
这比替他们做联网决定要好。

### 修复：发布 CLI 在 Python 3.10 上会全面启动失败

`update_check.py` 在模块层 `import tomllib`，而它是 3.11+ 才进标准库；发布 CLI 的壳声明
支持 3.10（`cli/pyproject.toml`）。该模块在**每个非帮助命令**分发前被导入，所以 3.10 上
不只是更新提醒失效——`setup`、转录、卸载全都直接 `ModuleNotFoundError`。

改成惰性导入并吞掉 `ImportError`：3.10 上只是文件级开关不可用，环境变量
`FINESUB_NO_UPDATE_CHECK` 照常。新增源码守卫
`test_the_thin_cli_stays_importable_on_python_310`——静态检查，因为本套件里没有 3.10 解释器，
而一条需要它才能跑的守卫等于永远不跑。

### 修复：更新提醒的版本比较不符合 PEP 440

两处：`0.5.0.post1` 相对 `0.5.0` 被判成「没有更新」，**漏掉正式 hotfix**；`1.0.0` 相对
等价的 `1.0` 被判成有更新，**造出假提醒**。现在保留 post 序号参与排序，并在比较前剥掉
无意义的末尾零。

### 修复：显式 `--device cpu` 时不该报显存不足

`gpu-vram-short` 原来在 `resolve_device` **之前**触发，压根没看设备——一台有显卡但用户
指定 `--device cpu` 的机器，会因为「显存不够」被警告一次，而那次运行根本不碰显存。
告警移到设备定下来之后，`--device cpu` 与 CPU 回退都静默。

### 新增 `[llm] proxy`：LLM API 请求走代理

一处设置，覆盖 LLM 层全部出网调用——Gemini REST、OpenAI 兼容 / Anthropic 端点、
媒体上传、Exa / Gemma4 / Tavily 检索、免费的 `countTokens`。

- 不写这一行 = 沿用 `HTTPS_PROXY` 等环境变量（0.5.0 之前的行为，不变）
- `proxy = "<url>"` = 走它，并忽略环境变量
- `proxy = "direct"` = 强制直连，并忽略环境变量

**只管 API，不管下载。** 实现上因此是一个显式的客户端工厂
（`finesub/llm/http.py`），而不是把代理写回 `os.environ`——环境变量会被下载族的
路由发现读到，那样就会连带把下载也改道，而这不是这项设置承诺的事。

工厂配两道检查。一条源码守卫：`finesub/llm/` 里除 `http.py` 外不得出现 `httpx.Client`，
**默认参数里也不行**——写它时立刻抓到了第五个此前没被列进来的出口（Gemini 断点续传上传）。
这正是显式接缝值得那 40 行的地方：漏掉一个出口两种做法都是静默的，但只有这一种能被查出来。

另一道是走真 socket 的证明：本机起一个极小的转发代理和一个源站，用请求行区分
（被代理的 HTTP 请求带**绝对 URI**）来确认请求真的改道了，而不只是参数传对了。
它**不进默认套件**——快且不出机器，但不该让每次 `pytest -q` 都去绑端口；
要跑加 `--run-network-mock`。

⚠ 开代理会改变失败的样子：Gemini 对机房 / 共享出口 IP 有风控，会直接拒答
（`LLMIPRiskError` 认得这种回复）。「检索能通、纠错一直失败」是这条设置下的正常现象。
本机 agent 不受影响——它们是自己登录、自己发请求的外部程序。

### CLI：命令跑完后提醒新版本

`finesub` 每天最多查一次 PyPI，发现更新的**正式版**就在 stderr 打一行并给出
`uv tool upgrade finesub`。只提醒，从不自动升级。

按 pip / `gh` / npm update-notifier 那套已经收敛的纪律做：

- **不阻塞**——查询在后台守护线程上跟命令并行，命令结束后最多再等 0.2 秒，没查完就用上次的答案。
- **失败完全静默**——没网、返回体不对、状态文件写不进去，统统一个字不打。
  一个能打印错误的版本提醒，比没有版本提醒更糟。
- **装完第一次运行不提醒**，那一次只把缓存种下（`update-check.json`，在共享数据目录）。
- **stderr、仅交互终端、`CI` 环境变量存在时不打**；`setup` / `uninstall` / `--help` 不打。
- 关掉：`FINESUB_NO_UPDATE_CHECK=1`，或 `config.toml` 的 `[cli] update_check = false`。

数据源用 PyPI 的 `info.version` 而不是 GitHub Releases：一次免鉴权请求，没有
60 次/小时/IP 的额度要分，而且 `info.version` 的定义就是「最新的非预发布版本」——
正好就是我们要的频道划分，不用自己实现。**预发布版本号（`0.5.0rc1` 这类）因此
天然不会被推荐**，`is_newer` 又在其上再挡一层。

### 路由预设：`agy` 拆成 `agy-hybrid` 与 `agy`

原来的 `agy` 预设混着两种东西：免费 Gemini API 打头，agy 的 Opus / Gemini 兜底。
名字没说这件事，想「只走订阅」的人得自己叠一个 `execution_policy = "agent-only"`。

- 原预设改名 **`agy-hybrid`**（名单不变），名称改为「agy 混合（免费 API 打头）」。
- 新增 **`agy`**：同样的格子，绑到新的 `agy-only-capable` / `agy-only-basic`
  两个组——就是原来的名单去掉 API 那一行。
- **`agy` 的 `test_target` 另指** `local-agy-media-gemini-3_7-flash`。
  继承默认预设那个免费 Gemini Lite 的话，`--test-profile` 会把整轮运行的每一次调用
  都钉在它上面——「纯 agy」的测试全跑去打 API，正是这个预设要避免的事。

写进 config 的是 preset 名，所以 `preset = "agy"` 的语义变了（现在是纯订阅）。
想保持原行为改写成 `preset = "agy-hybrid"`。

为什么用「新模型组」而不是「预设自带 policy」：policy 是**全局后端闸门**，
只能禁掉一个组已经列出的后端、永远不能添一个组没列的（`model_routes.toml` 里
写死的架构意图）。把 policy 绑进预设会让这条不变量变糊。两条路仍可叠加。

### 知识库：streamer 新增 `[问候语]` / `[结束语]`

登记在「频道用语」节（term 体、对外核查、随节共享）。定式台词正是「ASR 易错 + 有固定译法」
的那一类，而 term 行的四列恰好装得下「原句|定译|别名|什么时候说」。

**登记但不是 core**：不是每个主播都有固定问候语，留成 core 会让每个条目的完整预览
都多两行空槽提醒。登记的作用是共享策略——表外标记 fail-closed 成 `local`，
不登记的话跨库共享时会被丢掉。

### 知识库：登记的非 core 标记现在也渲染它的说明

完整预览过去把非 core 标记渲染成一行光秃秃的名字（`登记的其他标记：[X]、[Y]`），
而喂给知识任务的结构 fragment **只列 core 标记**。两处一凑，`core = false` 的实际
含义就成了「模型看得见这个标记，但无从知道该往里写什么、写成什么形状」——
`note` 字段在非 core 上是**死数据**。

现在它们和 core 标记一样带 note 渲染，只是仍不占空槽、不提醒。`common` 的
`[当前版本]` 因此第一次真的说出了自己那句「易腐信息；只在收录范围需要它界定时才写」。

### 知识库：`common` 自由节的说明注释不再逐节重复

`common` 的 `strict_sections = false`，每个未登记节名都解析到同一个 `default_section`，
于是完整预览把同一段用途/收录纪律在「角色」「地区」「组织」……下各印一遍——样板条目
《原神》里印了五遍。

那段话本来就不是在讲某一节，而是在讲「任何自由节能收什么」。现在它在全部小节之前
渲染一次，用一句「以下自由节（节名自取）统一按这条规则收录」划定范围。**具名节仍各自
带自己的那份**——那里的文字确实是节局部的。

只影响完整预览（用户与知识更新任务），纠错注入面逐字节不变，因此不 bump `PROMPT_VERSION`。

### `--gpu-budget-gb 4|8|12|16` 换成 `--gpu-tier entry|standard|high`，默认 `auto`

档位一直是**整卡显存**，但选项名（"budget"）和桌面端的「显存预算」读起来像
「允许 finesub 占用多少」。**数字本身就是那个歧义**：「8」既读得通「我的卡有 8GB」，
也读得通「只准用 8GB」，而两种读法选出的档位相反——16GB 卡填 4，语言裁判算出
装不下就静默掉到 CPU（慢很多）；8GB 卡填 16，分离器开四个实例然后 OOM。

所以档位改成按**显卡等级**命名，各自带着它的要求：

| 档位 | **需要空闲显存** | 内存 | 分离器实例 | 这一步 `auto` 选中它的卡 |
| --- | ---: | ---: | ---: | ---: |
| `entry` | **3GiB** | 8GB | 1 | 4–7GB |
| `standard` | **6.5GiB** | 8GB | 2 | 8–11GB |
| `high` | **10GiB** | 8GB | 3 | ≥12GB |

⚠ **最后一栏是这一步的取值。** 本版后来两次改了 `auto` 的落点（`high` 收紧到 ≥24GB、
12–23GB 改落新的 `standard_large_vram`），并新增了 `cpu` 档；档位共有五个而不是三个。
当前映射见本节更靠前的几条与《资源与大文件》。

**显存那一栏写的是「空闲」量，不是卡的容量**——桌面、合成器和驱动本来就占着一部分。
卡的容量只在 `auto` 定档时出现一瞬（减 `容量/8 + 0.5` 的预留后匹配前一栏），之后没有
任何地方见到它。把它当成要求，正是「我的卡有 8GB 所以装得下」变成 OOM 的那条路；
最后一栏是那个减法的结果，写出来只为让人对号入座。

- **默认 `auto`**（省略该选项同义），直接读驱动报的显存定档。桌面端下拉框每一档都写着
  自己的要求，标签由「显存预算」改为「显卡档位」。
- 定档先四舍五入到整 GiB 再向下取档。驱动从不报标称值——16GB 的 RTX 5070 Ti 实测报
  15.92 GiB，12GB 卡也只报 11 点几，直接向下取会把每张卡都降一级、白少一个分离器
  实例，而且不出声。
- 手填只有一个用途了：把卡让给别的程序。
- 没有可用 CUDA 设备时回落 `entry`，不额外告警（CPU 回退已经警告过一次）。

**去掉了 16GB / 4 实例的那一档。** 理由不是它多余，是它**更慢**：
`docs/separator-optimization.md` E7 的 worker 阶梯把吞吐峰值定在**两个** worker
（AOTI 1.098×、eager 1.048×），3 个降到 1.061×/1.046×，4 个再降到 1.050×/1.029×。
而且 4 worker 只在音频 **≥15 分钟**时才够得着（worker 数 =
`min(档位实例数, floor(时长/300)+1)`），那恰好是差距最大的区间。一个要多占显存、
换来更慢的档位不是档位。3 实例那一级保留——E7 出自单张卡，且 worker 数与分块规划
绑定，再往下砍会移动产物边界。

内存那一栏**三档相同不是漏填**：RAM 用量由模型加载与解码窗决定，两者都不随档位缩放；
随档位变的只有分离器实例数，那是 GPU 侧的计数。而且它是一条违规告警而非预留，
真要按档收紧得先有每档实测峰值。

**不做软上限**：CT2 遇 CUDA OOM 是进程级 abort，一个兑现不了的上限比没有更糟。

### `auto` 的预留改成随卡缩放：`容量 / 8 + 0.5`

原来是固定 1 GiB。它对 4GB 卡刚好，对 24GB 卡则明显太少——卡越大，卡上本来跑着的
东西（更大的桌面、更多显示器、更舍得缓存的驱动）也越多。

两个常数不是拟合出来的，是**选来让曲线正好穿过档位表**的：4GB→1.0、8GB→1.5、
12GB→2.0，减完剩下的正好是 3 / 6.5 / 10——三档各自的要求。这就是把它写成规则而不是
第四列的意义：同样那三个数，但现在延伸到了表里没写过的卡（16GB→2.5，24GB→3.5）。
匹配因此用 `<=`，否则每张标称卡都会掉一档。

**它仍然是探测余量，不是对真实占用的核算。** 本机对照（RTX 5070 Ti，桌面空闲）：
驱动报总量 15.92 GiB，`mem_get_info` 报实际占用 **1.24 GiB**，公式假设 2.50——
偏保守 1.18 GiB，是安全的那个方向（估少了空闲只会掉一档，估多了会 OOM）。
对照取自桌面空闲时；真开着浏览器或游戏占用会高得多。

### `entry` 档的说明改写：它是「基础 GPU 加速」，不是「弱卡专用」

加上 `auto` 的上限和新的显存告警之后，`entry` 那句「入门显卡」就有歧义了——它同时是
**最低的那一档 GPU 加速**，也是**显卡完全用不了时落到的那一档**，两件事挤在一个说法里。

现在的口径：**显卡受支持、且空闲显存 ≥3GB 时，`entry` 给你基础的 GPU 加速；否则仍然能走
CPU。** 三种情况都落到 `entry`，但后果不同：

| 情况 | 会怎样 |
| --- | --- |
| 显卡支持、空闲 ≥3GB | 拿到基础 GPU 加速，正常跑 |
| 没显卡 / 这版 PyTorch 不支持这张卡 | **自动回退 CPU** 并打 `Warning:`，能出正确字幕，只是慢很多 |
| 显卡能用但空闲显存不够 | **不自动转 CPU**（那会改变产物切分），打一条警告说清差多少，然后照跑 |

同步改了：`--help` 的档位说明（顺带把开头一句从「这是哪一级的卡」改成「让这台机器同时干
多少活」——档位在 CPU 运行上一样起作用，它决定分离 worker 数）、桌面端下拉的中英文案、
README 的档位那一条与「环境要求」一节，以及 `docs/manual/resources.md` 新增的
「显卡档位与 CPU 回退」一节（README 的文档索引指了过去）。`repo-install.md` 原来写
「需要 NVIDIA GPU」，也改成「没有也能跑，只是会回退 CPU 并慢很多」。

### 新增：显存不够时会说一声（`gpu-vram-short`）

此前有个真窟窿：**没有「显存太小就转 CPU」这条路**。没显卡或显卡不支持会干净地回退
CPU（慢但能跑完），但一张**能用只是太小**的卡不会——`resolve_device` 返回 `cuda`，
一切照常上 GPU，然后撞上 OOM。分离器那边是普通异常直接终止；ASR 走 CT2 更糟，
**CUDA OOM 是进程级硬中止**，连接都接不住。也就是说，一个本可以慢慢跑完的任务会直接崩。

两个 GPU 阶段现在各自在拿到 profile 之后、干活之前，拿驱动报的**当下空闲显存**和所选
档位的要求比一次，不够就打一条警告，给出「关掉占显存的程序 / 降档 / `--device cpu`」。

**只告警，不降档也不拒跑：**

- 降档会改变分离 worker 数 → 分块规划 → **产物边界**，同一个文件会因为「当时还开着
  什么」而产出不同切分。`auto` 读容量而不读空闲，正是为了让档位稳定；活数字不该从这里
  绕回来变成决策。
- **不够 ≠ 跑不了**：`entry` 实测峰值 2.26GiB，要求写的是 3GiB，略低于要求的卡照样能跑完
  不少素材。拒跑会拿走本来能成的运行。

**判据与位置对 `auto` 和手填一视同仁**——6GB 卡手填 `standard` 与经 `auto` 落到
`standard` 得到同一句话。用户点名要某个档位，欠他的是事实，不是替他改主意。
已在最小档时不建议「再降一档」（那句话在最需要出路时毫无用处），只留另外两条出路。
没有可用 CUDA 设备时不告警——那台机器在 CPU 路上，`resolve_device` 已经说过一次。

### `auto` 最高只选到 `standard`，`high` 需要卡报 ≥24GB

**有余量不等于该用。** `high` 跑三个分离 worker，而 E7 的 worker 阶梯把吞吐峰值定在
**两个**——三个是实测更慢的。原来的 `auto` 只看「装不装得下」，于是 12GB 及以上的卡
一律拿到 `high`：因为卡更大，换来一次更慢的运行，正好和档位的用意相反。

这一步的落点：

| 卡报的显存 | 这一步的 `auto` 选 |
| --- | --- |
| 无显卡 / 这版 torch 不支持这张卡 / 显存不足 | `entry` |
| <8GB | `entry` |
| 8–23GB | `standard` |
| ≥24GB | `high` |

`high` 始终可以手填。上限只会**往下压**，不会把小卡抬上去。

⚠ **上表是这一步的中间状态，不是本版的最终映射**：同一周期里后来又有两处改动——
无显卡的机器改落新的 `cpu` 档，12–23GB 的卡改落新的 `standard_large_vram` 档。
当前映射见本节更靠前的那两条，以及《资源与大文件》的档位表。

「显卡不支持」那一条之所以成立，是因为容量探针挂在 `cuda_usable()` 上而不是裸
`is_available()`——一张这版 torch 没有 kernel 的老卡会报「可用」然后跑不动。
显存不足的卡则是另一条分支：没有比 `entry` 更小的档可落，于是取 `entry`、由 OOM 告知，
和手填档位时的行为一致。

⚠ 12–23GB 卡上用 `auto` 的任务，分离 worker 数从 3 变 2，**产物边界会变**（重跑即可）。
本机 16GB 的默认档位因此不再是 `high`——按 E7 两个 worker 才是更快的那一档。
（这一步它落 `standard`；后来 `standard_large_vram` 出现之后落那一档，worker 数同样是 2。）

### 换卡后复测了三档显存/内存预算

`test_resource_budget_pipeline.py` 此前还挂在已退休的数字档位上（`parametrize([4])`），
默认被 skip 所以一直没红。修好并改成跑遍三档，然后在 RTX 5070 Ti 上真跑了一次
（700 秒素材——300 秒的 worker 阶梯要求素材过 600 秒才够得到 3 个分离 worker）：

| 档位 | 分离器 | VAD-ASR | GPU 阈值 | peak_mem | RAM 阈值 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `entry` | 2.26 | 0.87 | 3.00 | 2.43 | 8.00 |
| `standard` | 2.88 | 1.55 | 6.50 | 4.00 | 8.00 |
| `high` | 3.51 | 2.22 | 10.00 | 4.81 | 8.00 |

分离器曲线与 5060 Ti 的旧值一致，换卡没改变形状。**没有据此收紧任何阈值**，但它推翻了
一处此前写在代码注释里的说法：RAM **确实随档位缩放**（2.43 → 4.00 → 4.81，对应 1/2/3 个
分离 worker，每个 worker 都在主存里握着自己那块音频）。而且 RAM 才是贴得最近的那一个——
`high` 已用到 8GB 的六成，而 GPU 侧最坏也只用到 3.51/10。真要收紧预算，该先收 RAM，
且该按档分开。注释已改正。

测试现在无论过不过都把测得的数字打出来：一次重资源跑的价值就是那几个数，
让断言把它吞掉会使下一次标定从零开始。

### 资源违规告警改成人能读的单位

`peak_gmem exceeds tier limit (3639984783 > 2791728742)` —— 这条警告只说了
「超了」，超多少、超的是哪一档，都得读者自己按计算器。同一次调用前两行的 debug
行**早就格式化过**，唯独用户真正看得见的这条没有。现在是
`peak_gmem exceeds the entry tier limit (3.39 GiB > 3.00 GiB)`。

（发现方式：临时把阈值收到实测峰值附近，看报警长什么样。顺带确认了当前
3 / 6.5 / 10 的阈值下，只有 `entry` 抓得到 1.5 倍的显存回归，`standard` 与 `high`
全程静默——收紧到实测 +15% 后三档都抓得到，且实测峰值不误报。真要收紧得先在
5070 Ti 上复测，那批数字全出自 5060 Ti。）

⚠ 写进 `config.toml`、桌面偏好、任务记录与 manifest 行里的值全变了；原本用 16 档、
且音频 ≥15 分钟的任务产物边界会变（重跑即可）。产物 metadata 的 `gpu_budget_gb`
字段同时改名 `gpu_tier`（provenance，不匹配只告警）。`tools/` 里调用它的脚本已同步。

### 移除：搜索链末尾免 key 的 DuckDuckGo 兜底

本地检索代理的 search 降级链从 `Exa → Gemma4 grounded → Tavily → DuckDuckGo`
缩短为 `Exa → Gemma4 grounded → Tavily`。extract 本来就没有本地兜底，现在两条路一致。

删它不是因为它没用，是因为**它的失败长得像成功**：那一层是抓 HTML 再用正则解析，
DDG 一改页面结构就解析出零条结果，而「零条结果的成功」读起来正是「网上查不到这个」——
检索最不能编造的就是这个答案。现在所有 pool 都失败时返回错误结果，调用方看得见。

`config.toml` 的 `[providers] duckduckgo` 开关随之消失（留着它也没有意义）。
全部检索 key 都没配的用户，行为从「悄悄降级成低质量结果」变成「不检索」。

### 许可证：自 0.5.0 起改为 GPL-3.0-or-later

代码部分从 MIT 换成 [GPL-3.0-or-later](LICENSE)。`src/finesub/llm/prompt_templates/`
下的 prompt 明文不变，仍是 CC BY-SA 4.0。

**0.4.x 及更早的发行版仍按 MIT 授权**——已发布的授权撤销不了，也没打算撤销。
拿到 0.4.2 的人继续按 MIT 用它；这次变更只约束 0.5.0 起的版本。

⚠ 分发打包产物（Desktop portable / installer）时，GPL 要求随附或提供对应源码。
CLI wheel 自带 `_vendor` 源码快照，天然满足；桌面包靠 README 里指向对应
tag 的说明满足。

### 修复：中文 Windows 上显卡探测可能报「没有显卡」

桌面端列显卡时跑 `nvidia-smi`，那次调用只写了 `text=True` —— 于是用机器的 ANSI 代码页
解码。cp936 上只要驱动吐回一个它映射不了的字节，就抛 `UnicodeDecodeError`，而调用方
把任何异常都当成「没有 NVIDIA 驱动」：**一个错答案，长得跟合法答案一模一样**。
这里的输出全是给人看的名字，所以坏字节替换掉也比丢掉整份枚举强。

⚠ 这条是**外部贡献者 [@Ricori](https://github.com/Ricori) 报的**（[PR #14](https://github.com/caca2331/finesub/pull/14)）。
同一份 PR 里其余几处我们已经独立修过（ffmpeg/ffprobe 解码、Windows 上被句柄占住的
发布性重命名），另有三处它针对的代码路径已经不存在了（知识库转 SQLite 后不再调 git，
下载改走 yt-dlp 的 Python API）。**唯独这一处漏了**——而且不是没人想到：
仓库里本来就有一条「`text=True` 不带 `encoding=` 直接红」的源码守卫，
只是它的扫描面写的是 `src/`，从来没看过 `desktop/`。守卫的**筛选面本身就是守卫的一部分**，
现在它也扫 `desktop/backend` 了。

### 三类「安静地做错」现在会出声

都是**只报警、不修改任何输出**的检查。它们针对的是同一种毛病：出错的那一次和正常的
那一次，从外面看**长得一模一样**——文件写出来了、格式合法、字幕能播，只是内容不对。

- **词时间轴坍塌**（`alignment-collapsed`）。对齐从不大声失败：如果它退化成「所有词
  都在 0 秒」或者「整段话被压进半秒」，返回的仍然是成功。五个各自独立的信号，任何一个
  触发就报——每个都带着自己的前提条件，免得在一段本来就很短的录音上乱叫。
  **看的是产出的时间戳，不是产生它的代码。**
- **时间倒流**（`timeline-out-of-order`）。全局 DP 重分句、稳定化重写、LLM 纠错这三处
  都会重排或替换分段，而在此之前**没有任何一环检查过顺序**。
- **SRT 自检接上了管线**（`srt-invalid` / `srt-line-budget`）。校验器本来就在仓库里，
  但一次都没被调用过。

三条都**不做自动修复**：我们从没量过它们在生产里多久触发一次，而一个没量过的自动修复
只是把一个看不见的错答案换成另一个。

### `--asr-decode-batch`：旋钮在，组批还没实现

`auto`（= 当前档位的取值，各档都是 1）或一个整数。⚠ **填大于 1 的值不会加速**：
组批本身还没做，管线会记下这个值、打一条 `asr-decode-batch-inert` 警告，然后照旧
一个窗口一次解码。放在这里是为了让「已记录但未生效」这件事**说出口**，而不是让人
以为设了就生效了。

⚠ **本版后来实现了组批**（见本节「`--asr-decode-batch` 现在真的会组批」），
这条描述的是它落地之前的状态；`asr-decode-batch-inert` 那条警告随之不再出现。

### 可以把知识库的名字告诉 ASR 的第二模型了

`--asr-context off|terms|full`（默认 **off**）。选出来的词条来自视频标题和你自己写的
`--extra-info`，用的是纠错层本来就在跑的那套检索。

- `terms` —— **只给名字**：条目名、术语的原文/中文定名/各种别名，**不给一句话描述**。
  识别器需要的是「这些字串要拼对」，不是解释。
- `full` —— 与纠错层看到的一样。

多输入跑法也认这个开关（命令行 `--asr-context`，manifest 里的 `asr_context`）——
同一条 `finesub`，见下面「一个入口」。

⚠ 默认关着：它改变第二个模型听到的内容。已经量过（术语命中 3.4% → 30.5%，
而且注入的两百来个名字里只有该出现的那一两个真被写出来），但同时它也改动了八成的
输出——虽然更近的比更远的多 3.6 倍，这些数比的仍是「和纠错层写的一样吗」，
不是「听对了吗」。

### 知识库预注入现在也认「条目里的词条」

以前只看「这个**条目**被提到了吗」，而条目名是作品名和主播名——很少有人真把它说出口。
实测 20 个真实任务里，命中的 351 次有 **347 次是条目里的词条**（角色名、专名），
其中 **87% 的母条目从头到尾没被提过**——那些以前完全看不见。

命中一个词条只注入**那一行 + 它属于哪个条目、哪一节**，不再把整篇拖进来；
别名和误听写法一起参与匹配（片假名/平假名写法不影响）。

### 极低电平的区间不再送去转写

绝对电平（真 dBFS）低到一定程度的区间，现在在解码前就被丢掉，**默认开**。
判据是**峰值与功率均值同时**低于门限（−60 / −70 dBFS）——两个条件缺一不可，
因为耳语的动态范围被压扁，只看峰值会把真耳语一起吃掉。

标定是单向压下去的：一路降到**耳语语料一条带文本的区间都不丢**，再往下留 5 dB。
在 42 份生产录音上它拿掉 **0.32%** 的解码时间、194 个区间，其中 8 个有文本，
**这 8 个全部交给第二模型验过、全是幻觉**。

⚠ **这不是提速**，0.32% 在测量噪声里；它拿掉的是幻觉的燃料。

再往上一档（没那么安静、可能是噪音的那些）**只打标记**，不丢。标记会不会真的变成
第二模型的一次推理，跟随 `--qwen-verify` 的开关——关着就一分钱不花。

### URL 标题：只在用得上的时候抓，而且只抓一次

两处收紧，都是为了「重跑不上网」：

- **只有会跑到纠错/翻译阶段的运行才抓标题**。默认的 `--stage raw-srt` 没有任何东西
  会读 `extra-info`，以前却照样为它跑一趟 yt-dlp。
- **抓不到也记下来**。以前抓失败（B 站限流会回 412）就什么都不写，于是**每次重跑都
  再探一次网**。现在「问过了，没有」会被记进缓存，重跑直接读缓存。
  想重新抓：删掉 `data/reference/url-info.json`。

### 删掉了「VAD 结果可疑」这条警告

以前，一份长录音如果几乎找不到语音，会打印一条 `vad-implausible` 警告。删了。

理由是它触发的那个情形——**整份文件的语音少于 1%**——意味着你拿到的字幕本来就
几乎是空的，打开一眼就看见；而警告让你「检查增益、采样率和能量门限」，
这些在本工具里**都不可配**，所以它既没有告诉你新东西，也没有可做的事。

运行元数据里的 `audio_coverage` **保留**（它只是记录，没有阈值）。

### 幻觉套话清单补上了英文

Whisper 在几乎无人声的音频上会反复编出套话。以前只认三条日文
（`おわり` / `それではまた` / `ありがとうございました`），英文一条都没有，
所以 `Thank you.` / `Thank` / `I'm sorry.` 这类会一路留到成品字幕里。

现在英文也会被送去第二个模型复核；模型说「这里没有这句话」才删。
拿一份壁炉环境音的实测：清掉的幻觉从 42% 提到 **76%**。

⚠ **英文只走这条复核腿，不走「语速不可能」那条**。日文那条腿的门槛是 20 字/秒——
对中日文是物理不可能，对英文却只是正常快语速（约 200 wpm），套上去会误删真话。
另外单独一个 `you.` 故意不认（三个字符会命中真实代词）。

### yt-dlp 升到 2026.8.19

2026.7.4 那版在 YouTube 上会全部 403。

### URL 输入会自动带上视频标题

从 URL 跑的时候，`extra-info` 里会在 URL 那一行下面多一行标题：

```text
视频来源 URL: https://www.bilibili.com/video/BVxxxxxxxxxx
视频标题（yt-dlp 自动抓取，未经人工核对）: 【原神/熟肉】…/四月一日ベレト
媒体文件: out/BVxxxxxxxxxx/BVxxxxxxxxxx.mp4
```

**标注是刻意的**：这一行是从网页上抓来的，不是我们算出来的、也不是你写的，
所以它标明了来源，不裸贴。抓不到就没有这一行，**不会因此让转写失败**
（离线、链接失效、被限流、没装 yt-dlp——一律安静跳过）。

为什么加：纠错层最缺的信息是「这期讲的是什么」，而管线以前什么都没记。
标题通常同时带上了作品名和主播名，是最便宜的补法。

**视频简介不抓**——里面常混着赞助语和频道模板，会把无关词条预注入进来。

### `--lang-redecode on` 现在还会做一次全局语言核对

原来的语言检查只问一件事：「这一窗的语言和最近几窗的多数一样吗？」
所以**整条录音一起判错时它是安静的**——多数本身就是错的，永远矛盾不起来。
实测过一次：89.7% 的日语被判成英语，检查一次都没触发。

`--lang-redecode on` 现在会额外抽查本次录音的 8 个片段，交给第二个模型重新听一遍，
两边对「这条录音主要是什么语言」的判断不一致就报警。**只报警，不会自己改语言。**

它也让 `--lang-redecode on` 在 `--language` 下不再是空转：你**把语言指定错了**，
现在会被说出来（以前完全没有任何提示）。

⚠ 两条要知道的：
- 它抓的是「**整条**判错」。真正的双语录音（一半日语一半英语）它抓不到，
  而且在这种录音上**它反而可能报警**——两个模型各挑了一半，谁都不算错，忽略即可。
- 一轮要 ~17 秒，**所以默认（`auto`）不做**，要显式写 `--lang-redecode on`。

### ASR 略快一点：encoder 输出在输入逐字节相同时复用

`language` 留空（默认）时，faster-whisper 会先编码一遍来猜语言、把结果丢掉，
再编码同一段音频去解码；覆盖率 beam 救援同样会用同一份音频重跑。现在这两种情况下
第二次编码直接复用第一次的结果。**产出完全不变**——命中条件是整份特征逐字节相同，
而 encoder 是纯函数；已按 segments / 时间轴 / 确定性 metadata 逐位相等验收。

23.5 分钟素材上省掉 18 次编码、1.6 秒（识别时间的 3.6%）。填不满 30 秒的窗口拿不到
这个收益，因为上游那两次编码的取值范围差一帧，不是逐字节相同的。

### 运行元数据新增两个诊断字段

`<stem>-metadata.json` 里每个阶段多了 `cpu_sec` 与 `wall_cpu_ratio`，
另有一个 `audio_coverage`（送进模型的语音时长 / 总时长、区间数）。
**两者都只是记录，不参与任何判断**：挂死与否仍由既有的 stall watchdog 决定，
VAD 覆盖率过低仍由 VAD 兜底谓词判断。它们存在是为了这些链**退化时能被看见**。

⚠ 顺带一提实测：健康运行的 `wall_cpu_ratio` 是 **0.5 上下**（阶段是多线程的，
累计 CPU 超过墙钟），不是 1。

**两处覆盖面修正**（都会让你看到以前看不到的东西）：

- **复用旧 VAD 产物的运行现在也会被检查。** VAD 兜底谓词此前只判新算出来的区间，
  跳过 VAD 直接复用 `*-vad.json` 时完全不判——而复用恰恰是没有别的东西会再看它的
  时候。现在两条路都判，所以一份早先跑坏的 VAD 产物会在**复用时**报警，而不是一直
  安静地被用下去。
- **一段语音都没找到的运行现在也记 `audio_coverage`。** 它以前记在「无语音」提前
  返回之后，于是最该被记录的那种跑法恰好没记。

### 字幕行超长的提示不再重复三遍

原始字幕导出会往同一个临时文件写最多三次（渲染一次，每个时间轴 profile 各一次），
过去每次都校验，于是同一条「行超长」提示会打印三遍，而且指着一个用户看不到的
`.part` 临时路径。现在中间几趟不出声，成品文件校验一次，提示里给的是真实文件名。

⚠ 这条起初只改到了一半：纠错阶段自己也有一份同样的循环，`--stage final-srt` 的运行
因此仍然每条提示打三遍（识别那半一遍，纠错那半按时间轴 profile 数再两遍，后两遍还
用的是绝对路径）。2026-09-03 发版预演实跑时发现并补全。

### `--vad-silero-assist` 默认开启，并改由后端 resolver 决定

桌面任务从 0.3.x 起就恒开它（每个任务必经分离器，而这个两信号后置组合正是为那种
残留噪声的人声存在的），CLI 却一直停在 opt-in —— 标定早就做完了，只是默认值忘了翻。

现在三个入口一致：默认**开**，`--no-vad-silero-assist` 关闭，`config.toml` 的
`[vad] silero_assist` 可覆盖。解析链只写在一处
（`vad_asr_stage.resolve_vad_silero_assist`：flag > 配置 > 代码默认），
argparse 传 `None` 表示「用户没说」，桌面端不再自带第三份副本。

**行为变化**：未显式传过这个 flag 的 CLI run，VAD interval 会与以前不同（这个开关
改变的是 interval 本身，不只是怎么找到它们），下游 `-aligned.json` 起随之变化。
**干净的、未经分离的源音频建议显式 `--no-vad-silero-assist`** —— 那种素材上打包
扰动是白付的。成本在流式化之后约为 60min 素材 +3s(CPU) / +1s(CUDA)。

### VAD 前缀：`vad_silero_assist` 从复用键改为 provenance

翻这个开关不再让同一任务里已有的 `*-vad.json` 被判失配并重算。它是用户选择的运行
参数，不匹配既不会让续跑报错、也不会让数据拿不到，所以它记在 `provenance` 里、
不匹配时只 warning，产物照常复用；要全量按新参数重算，新建任务。
复用键只剩音频身份（名字/大小/mtime），比较改成按子集比而非整字典相等——
后者会让**每一份升级前写下的 prefix** 在首次运行时因「旧 4 键 vs 新 3 键」被作废，
正是这次改动要消除的行为。

### 跑着的时候可以加任务、插队、撤掉——不用事先准备 manifest

有批次 id 的运行会在 `out/batch/<id>/` 下放两份文件（启动时打印路径）：`queue.jsonl` 由运行器
维护，是这一批的现状；`control.jsonl` 由你追加，是控制面。往控制面追加一行 `{"source": ...}`
就是加一个任务，`{"item": "x", "priority": 9}` 让还没开始的项插队，`{"item": "x", "drop": true}`
撤掉还没开始的项。两份文件各只有一个写者，你写你的、它写它的，不会互相覆盖。

这也让**位置参数跑法**（`finesub a.wav b.mp4`）第一次有了「加任务」的地方——此前只有
`--manifest` 跑法可以追加。改优先级会重排 ASR 队列，所以已经在排队的项也会跟着动；已经开始的
项不会被打断。

`queue.jsonl` 同时是这次运行的记录：跑完或中断都留着，它的行就是合法的 manifest 行。它的
`_state` 现在真的分 `queued`/`running`（在跑的项另带 `_stage`），因此 `drop` 能准确区分「还没轮到」
与「正在跑」——此前一个下载了三分钟的项也会被当成没开始而撤掉。撤掉是永久的：记成
`_state: dropped`，续跑不会把它重新拾起来（因中断或失败没做完的项才会），整批只剩被撤掉的项时
算跑完——包括续跑一个只剩撤掉项的批：直接结清返回 0，而不是按「没有输入」报错、让它永远停在
未完成。

### `--resume-batch`：接着跑上一个没跑完的批，不用记 id

`finesub --resume-batch` 取最近一个没跑完的批（包括被强杀、没来得及写收尾的）继续；
`finesub --resume-batch <id>` 指定某一个。它查的是 `<用户数据>/batches.json` 这份**指针**
（批次 id、queue 文件在哪、什么时候更新的），不存任何任务。

**超过 7 天的不会被默默捡起来**：会告诉你那批多久没动了，并打印带 id 的确切命令，由你决定。
不做交互确认（会卡住脚本），也不静默继续（一周前的东西突然接着跑正是该防的意外）；显式给了
id 则不受年龄限制。不想接着跑仍然是什么都不做。

**续跑就是那个批本身**：同一个 id、同一个 `out/batch/<id>/`、同一份 `queue.jsonl` 与
`control.jsonl`、同一条记录、同一把锁。所以它跑完就是那个批跑完了，下次不带 id 的续跑不会再挑中
它；`--batch-id` 因此不能与 `--resume-batch` 同给。续跑期间新任务从 `control.jsonl` 进（那份
manifest 已归运行器写）。

**续跑时你亲手打的选项会盖过记下来的值**，没打的照记录来：`finesub --resume-batch --device cpu`
现在真的会换成 CPU（此前记录里的 `device: cuda` 静默胜出，那批在丢了显卡的机器上就再也续不动）。
判据是命令行上出现过没有，不是值等不等于默认值。覆盖会写回 `queue.jsonl`，因此是粘的；`--stage`
往前推会连已经跑完的项一起带上，启动时告诉你有几项因此复活。这条只对续跑的行成立——你自己那份
manifest 和 `control.jsonl` 里的行仍然是行覆盖命令行。

**控制面的进度是持久的，且只在指令真正生效之后推进**：追加了一行、还没被轮询到就被强杀，那一行
下次续跑仍然会执行——写在盘上不等于已经做了；已经生效过的行不重复执行。

批次记录的键是 **(目录, 批次 id)**：`--batch-id nightly` 在两个目录里各跑一次，或两个批在同一秒
启动，此前后者会把前者从全机记录里抹掉、于是那个批再也无法 `--resume-batch`。不带 id 的续跑也
只在本目录的批里挑，别处有而这里没有时会告诉你 `cd` 去哪。

另有两种情况**给了 id 也拒绝**：那批**还在跑**（它持着 `out/batch/<id>/.batch.lock`，续跑会变成
两套 worker 写同一批产物——要加东西请用它的 `control.jsonl`），或那批**是在另一个目录里跑的**
（`out/` 与相对路径都是相对当前目录的，换个目录续跑既找不到上半场、也会把下半场写到别处；
它会告诉你 `cd` 去哪）。同一把锁也挡住了「同一个 `--batch-id` 同时跑两次」——撞上时批量跑法
被指去它的 `control.jsonl`，单输入跑法则被建议换个 id（它没有控制面可指，此前这个分支会直接
报 `NameError` 而不是给出提示）。

**任务侧只是加了一句话，没有加拦截**：重跑同一个源文件时，若要复用的产物超过 7 天没动，
`Continuing task ...` 会带上 `(last run N days ago)`。不拦是因为这里是你自己指名了源文件，
而「接着上次继续」就是 exist-skip 重跑本身——拦它等于拦掉这个特性；不带 id 的批续跑挑的是
你没点名的那一批，那才是需要防的意外。

### 修复：装好的 `finesub` 跑多个输入会直接退出

`finesub a.wav b.mp4`（打包版命令行）此前必然以退出码 2 失败：外壳把每次运行都当成一个任务、
给它追加了一个 `-o`，而那个选项在多输入时会被管线拒绝（它会让每一项写到同一个位置）。
`--manifest` / `--resume-batch` 也会打印一句「源文件必须放在最前面」的误导提示。这是
`finesub batch` 子命令取消（0.4.0 起改为多输入即批量）时漏掉的一半：批量运行不属于任何单个
任务，产物按 `out/<名字>/` 落在当前目录、不进 `tasks.json`，现在启动时会说明这一点。

### 多输入：优先级、失败重试、worker 不再被一个异常带走

- manifest 行新增 `priority`（数字，大的先跑，默认 0）：同时作用于 ASR 优先队列与 LLM 调度。
  给增长型批准备的——批跑到一半追加一个急件，此前只能按「大的先跑」排队等。
- 失败的**阶段**默认自动重试 1 次（`--retry-failed N`，0 关闭）。重试的是阶段不是整项：它前面
  的阶段产物已经在盘上、本来就会被跳过，所以只赔失败的那一段。重试进 batch-status 的 `retry` 事件。
- 一个任务抛出非 `Exception` 的错误（库里的 `SystemExit` 之类）此前会终结所在 bin 的 worker
  线程；asr bin 只有一个 worker，于是队列填满、整批卡住。现在按普通失败处理，bin 不丢 worker。

### 新增使用者文档：一次跑多个输入

`docs/manual/batch.md`——manifest 行能写什么（任何管线选项 + `group`/`priority`）、**跑着的时候
怎么往里加任务**、为什么两项不能共用产物位置、失败/中断/重跑的语义、事件流与逐项日志在哪。

### 知识库并发冲突会回喂给模型重判（B'）

并发写入抢先落库、你的提案被按类型丢弃之后，harness 追加**一轮**修复调用：告诉模型它读的是哪
一版、现在是哪一版、哪几条因为什么被丢，并把相关条目**当前版本**的内容连同该版本的句柄一起给
它，只让它重判这几条——别人已经写了等价内容就跳过，输出空块是正常结果。回喂轮不再给一遍材料
（看不到证据还要补充等于编造）。整个环节尽力而为：主提案早已落库，回喂失败只打告警，运行照常
成功（失败那轮的 exchange 也照记，便于回看）；一次冲突只回喂一轮，最多 6 个条目。只有真有**别的
任务**写进来才回喂：模型自己两行相撞造成的丢弃不再多花一轮调用。

### 多个输入时每一项也有自己的 run 日志

`<user-data>/logs/run-<时间戳>-<输入名>.log` 此前只有单输入前台跑才写。多输入才是没人逐行盯着
的那种运行，现在每项一份：文件在该项第一次说话时才创建（没跑起来的项不留空文件），basename
相同的两项加数字后缀而不是挤进同一个文件。

### 一个入口：`finesub` 直接收多个输入，`batch` 子命令取消

批量不再是另一个命令。`finesub a.wav b.mp4`、`finesub --manifest tasks.jsonl` 与
`finesub a.wav` 走的是同一条路——一个输入就是「N=1」的一项，同样经过三 bin 流水线，只是前台
呈现不同（进度原地重绘、失败打栈、写 run 日志、默认不写 batch-status）。源码安装对应
`python -m finesub.pipeline`；`finesub batch` 与 `python -m finesub.batch` 已移除。

由此带来三点用户可见的变化：

- **管线的每个选项现在都能逐项覆盖**。manifest 行可用的键从 `run_pipeline` 的签名导出，
  `--qwen-verify`、`--word`、`--lang-redecode`、`--split-length-scale`、`--vad-silero-assist`
  五个此前只有单文件能用的选项，现在写进 manifest 行即可生效。
- **多输入时 `--stage` 的默认值改为 `raw-srt`**（原批量默认 `final-srt`），与单文件、与文档
  「LLM 阶段 opt-in」的口径一致：不显式要求就不花 LLM 额度。要纠错翻译加 `--stage final-srt`
  或 `--llm-correct-translate`。
- **输入路径不存在时立即报错退出（码 2）**，不再等到进入管线才抛栈——原本批量就有的投喂前
  预检，现在单文件也有。同一道预检还拒绝**会互相覆盖的产物**：两个输入算出同一个 SRT 路径
  （`dir1/a.wav dir2/a.wav` 同名，或同一个源列两次）直接报错，而不是让后一项按「产物已存在」
  跳过全部阶段、拿着前一项的字幕报成功。
- **属于某一次运行 / 某一个素材的选项在多输入下被拒绝**：写入位置与运行身份
  （`-o/--output`、`--name`、`--task-id`、`--task-artifact-dir`）与逐素材的只读输入
  （`--llm-video`、`--refined-srt`——广播出去不会覆盖文件，但会让 B 拿 A 的素材做纠错、
  并把结果写进知识库）只在单输入时作为默认值，多输入请写进 manifest 行。整批共用的提示
  上下文 `--task-summary` 不在此列，照常继承。
- 产物唯一性检查覆盖**每一道准入**：启动项、运行中追加的 manifest 行、以及 URL 下载解析出
  真实路径的那一刻（后者只让该项失败，不拖累整批），比对最终 SRT 与 LLM 产物目录两个写入根，
  路径按大小写归一。
- **Ctrl-C 第二次立刻退出**：第一次仍是「不再开新任务、等手上这段收尾」，但第二次不再被吞掉。
  统一入口后单文件也走 runner，只保留第一种语义等于让数小时的阶段无法中止。
- `FINESUB_LOG_LEVEL` 恢复生效（单输入与多输入都是）：级别的解析收敛到 `resolve_log_level`
  一处，之前前端把未指定提前解析成 `normal`，环境变量因此对所有路径失灵。

### batch 运行中可以追加任务

`--manifest` 的批在运行期间轮询 manifest 的**追加行**：往文件末尾追加新的 JSONL
行（数秒内生效），新任务加入当前批的下载→ASR→LLM 流水线，batch-status 记 `added` 事件。
append-only——修改已消费的行无效；只有带换行结尾的完整行会被读取，写到一半的行等写完；坏行
打印警告后跳过，不影响运行中的批。批在「所有已知任务完成且最后一次轮询**确定**无新行」时正常
退出，之后追加的行由下一次运行接手（与 resume 一致）——正在写入的尾行或一次读取失败都不算
「无新行」，批会多等几轮让它写完（连续 3 轮仍未完成才告警退出）。

### 并行默认值：任务间开、任务内关（windows 1 / tasks 2）

`--llm-parallel-windows` 默认从 4 改为 **1**，`--max-parallel-tasks` 默认从 1 改为
**2**：任务之间并行（互不共享上下文，无质量代价），任务之内默认串行（保留 advice 台账与前缀
缓存收益）。原有行为可通过显式传参恢复。三个旋钮都由用户覆盖，但**不彼此独立**：保底 agent 槽
在任务起步就占住，所以 `[llm].local_agent_max_parallel`（默认 4）必须不低于
`--max-parallel-tasks`，否则多出来的任务会卡在起步处等前一个任务整体结束。

reference_ingest 不再特殊对待：任务不带调度组、直接吃 batch 的并发默认值，与其它批一致。
代价是批内知识累积不再保证「后一条能读到前一条刚提交的词条」——素材通常互不相关，而写库
自 W2 起本就支持并发写。

### 并行路径的六处修正（2026-08-30 复审）

- 知识库并发：竞争提交恰好落在校验与写事务之间时，事务内化解掉全部操作后仍会提交一个**零内容
  revision** 并报成功（历史里多出幽灵版本、按版本号判断「有没有变化」的地方全错）。现在这种
  情况与事务前化解一样返回 `rev=None`。
- 超长素材的分块调查在 conversational 后端下会并发派发到同一个人的单条 agent 队列——纠错阶段
  的「强制串行」发生在研究之后、只看纠错格，覆盖不到它。现在研究阶段按自己的路由独立判断。
- `--manifest` 启动时读了两次 manifest：两次读取之间追加的行会被记为已消费却
  从未构建，本次运行永远不会跑它。改为单次快照同时供给解析与追加游标。
- reference_ingest 的知识更新步骤此前不占任务槽位账户；任务级并行下它会失去保底 lane，
  现在与纠错任务一样各自记账（嵌套调用复用同一份，不重复预留）。
- 知识库建表/迁移用的是延迟 `BEGIN`：两个线程同时开同一个 store 时各持读锁再争升级，
  SQLite 对这种升级死锁**立刻**返回 `database is locked`（`busy_timeout` 不介入）。并行任务
  与线程池 worker 每人一个连接，正是这个场景——改为 `BEGIN IMMEDIATE`。
- 知识库出处记账移进写事务内：它此前在提交之后逐行 INSERT，绕开单写者队列抢写锁（并发 apply
  下会 `database is locked`），也是唯一可能在 revision 已提交之后失败、把成功的 apply 报成
  异常的写。

### 任务级并行的地基与旋钮（task-parallelism plan W1–W6）

LLM 层从「一进程一次一个任务」走向可并行。用户可见的变化：

- 新增 `--max-parallel-tasks`（默认 1，行为不变）：允许多个任务的 LLM 阶段
  同时运行。manifest 行新增 `group` 键：同组任务（同视频分段、同语料批）严格按提交顺序
  串行；组间由调度器按「长任务先行」派发，实际顺序记录在 batch-status 的 `llm-scheduled`
  事件里。
- `config.toml` 新增 `[llm].local_agent_max_parallel`（默认 4）：本机同时活跃的 agent CLI
  进程数上限，全部本机工具共用一份预算；每个任务保有一条不会被别的任务挤掉的「保底 lane」。
- 并行任务各读各的知识库快照，互不等待；并发提交的真冲突按类型化解（改名回退、新建同名连
  依赖一起丢、其余提案照常落库），冲突出现在 task report、`knowledge-apply-conflict` 告警与
  batch-status。并行纠错在屏障处重读当前知识库版本，长任务期间别的任务新交的词条对纠错阶段
  可见。
- 绑定 conversational 后端的纠错格若配了 `--llm-continuity parallel`，会强制回 serial 并
  告警：一个人的 agent 只有一条队列，并行只会丢掉 advice 台账而不省墙钟。

### 超长素材的背景调查改为分块（默认开启）

调查 transcript 超过 `[chunking].research_transcript_max_tokens`（默认 100,000 tokens，`0`
关闭）时不再直接报错「先切分音频」：研究阶段按整数个调查窗口切块，每块独立跑完整的
r1 + 搜索 + r2，产物按块落盘、断点按块重放；逐窗笔记按素材区间拼合，各块的全局结论合并为
一份（带固定的分歧处理说明）注入每个纠错窗。`--llm-continuity parallel` 下各块并行执行
（受 `--llm-parallel-windows` 与任务级槽位预算约束）。阈值以下与 `--llm-retrieval native`
的行为不变。

### dsh 的工具事件不再是盲区，native 搜索也真能用了

dsh headless 只往 stdout 打最终答案，driver 因此一直声明 `observes_tool_events = False`。
但它的 session-persistence 插件从来都在写完整 transcript——`tool/call`、`tool/result`、
参数、返回、reasoning 一应俱全，只是落在 `$DSH_HOME/sessions` 且压成 zstd 帧。

现在 driver 用 patch 把该插件的 `root` 指进本次 capsule、`compression` 设为 `none`：
既不往用户自己的会话库里混写（插件 README 明说一个 root 只能一种编码），也不引入 zstd
依赖，transcript 还随 capsule 一起清理。`observes_tool_events` 随之置 `True`，dsh 拿到
与 codex/claude 同级的 `search_events`。

同时修好 native 搜索：dsh 的搜索后端从环境读自己的 provider key，而 driver 的环境白名单
把它剥掉了，于是 `--retrieval native` 给了模型一个每次都报「no API key for
DEEPSEEK_API_KEY」的工具（实测模型试两次后如实回答自己搜不了）。该 key 是账号自己的、
不是 harness 的，现在只在这个 driver、只在检索被授权时、只放这一个名字。

### 修复：agy 的 native 搜索在 tool-session 传输上被静默拒绝

`--llm-retrieval native` 配 agy 时，研究轮的联网搜索一直是无效的。研究轮不带媒体，按传输
规则走 tool-session，而那条路上只有一个不带检索授权的 project：模型按会话 protocol 的指示
调 `search_web`，project 的 PreToolUse hook 落到 deny 默认拒了它，**被拒的调用不产生任何
结果步**，模型于是安静地改用知识库继续。产物侧同时看不见：`search_events` 只在工具事件
`state == "DONE"` 时才记，被拒的调用是无名的 `ACTIVE -> ERROR`，两个条件都不满足。

现在 tool-session 与 capsule 一样按授权分成两个 project（`.finesub-tool-native-<slot>`），
两份 guard 由同一模板派生以防漂移，四份 guard/agent document 的 sha256 全部进 execution
identity。worker bootstrap 第 4 条原本写死「不要使用 `finesub` 以外的任何工具」，与 protocol
的联网指令直接冲突，现在授权时会嵌入例外句——嵌在同句之内，因为 agy 会把 bootstrap 压成
摘要。`native_search_not_used` 这条 note 的措辞也从「模型没搜」改成陈述观察到什么。

同一处还有第二个闸门：agy 在 `read_url_content` 之前要一条 permission，headless 模式无法
弹窗、没有 grant 就自动拒绝，**并且整个 turn 就此结束**——一次被拒的抓取会连带丢掉它前面
已完成的搜索。`search_web` 不需要这条 grant，所以在模型「只搜不抓」之前授权看起来是完整的。
现在 native 的两条路（capsule 与 tool-session）都会把 `read_url(*)` 写进 project 自己的
记录，用户全局 settings 依旧不碰。

影响：此前用 agy 跑的 `retrieval=native` 任务，研究轮的专名都没有经过联网核实。

实测同批核对：codex 的 native（`--search`）与 local（harness 代理）两档、dsh 的 local 档
均正常计数并带 URL；dsh 的 native 工具确实在（自报工具表含 `web_search`），但 dsh headless
不输出工具事件，`native_search_unobserved` 是其固有状态而非缺陷。

### 知识库改用 `[标记] 行体` 行文法，条目结构随之重划

条目里的每一行现在都是「可选的方括号标记 + 行体」，例如 `[自称] ワタヌキ|我|わたぬき|…`。
方括号本身就是消歧符：**冒号不再有任何语法作用**，`《崩坏：星穹铁道》` 这类标题不会再被
当成字段名切开。行的类型由行体自己的形状定——四段以上竖线就是术语行，其余是普通说明行，
恰好三段仍然报错。`fact` / `event` / `relation` 三种节点类型退休：它们各自的格式化引导现在
只是行首的一个标记，能读、不再新写。

**标记名不需要事先登记**：`[出道] 2022-12` 这类自定义标记合法。preset 里登记的那些只是
额外携带元数据（哪个标记是本名、哪个是别名、哪些跟着条目一起共享）。未登记的标记一律
不共享、不外发核查。同一小节内标记唯一——想改一行就 update 它，不要再追加一行同名的。

主播条目的小节重划为 档案 / 直播内容 / 频道用语 / 特点 / 人际关系 / 待归类：「重要经历」
退休（版本性叙事会过期，对译文也不起作用），「说话风格」并入「特点」并拆成 `[语体]`（文字
线索：敬体常体、方言语种）与 `[音色]`（声音线索，多模态任务据此区分说话人）；称呼类信息
（自称、他称、粉丝名）统一归「频道用语」写成术语行；人际关系也改成术语行——那些名字同样
会被 ASR 听错。「待归类」是新的暂存区，转运站而非仓库，非空就在报告里出信号。

条目预览分两档，与人读/模型读的区分正交：**完整预览**（你自己编辑时、以及知识更新任务）
渲染出全部小节（空的也渲染）、每节的收录说明、以及尚未填写的核心标记空槽——模型得知道
有这些位置可以填；**部分预览**（纠错翻译等其余任务）三样都不渲染，只给有内容的行。空着
本身就是提醒，库里不会为此存任何空行。

`[别名]` 行渲染自检索索引而不是一个独立节点，因此**删掉整行不等于清空别名**；要清空就把
行留着、把内容删干净。agent 的 `kb_read` 去掉了 `tier` 参数，改为按 `sections` 取。

老库会在首次打开时自动重导并转换（转换前后逐行比对，不一致就拒绝启动而不是带着损坏继续）。

### 知识库术语行改固定四列；维护任务不再静默丢信息

术语行从「三/四段可选别名尾列」改为**固定四列** `源语言|中文定名|别名|一句话描述`：别名列
永远在第三列（顿号分隔、无别名留空），描述在最后、可以含竖线。这消灭了两类静默数据损坏——
模型整行改写时漏写尾列会把别名全删掉、描述里的竖线会被旧五段语法错拆进别名/读音。恰好三列
的行现在报格式错误而不是悄悄降级。`rendered/` 下已有的人工编辑不受影响：升级时会先按旧格式
收割你的改动、再以新格式重渲。apply 报告现在按引擎实际结果生成（被拒的操作不再显示为已应用），
别名的每条增删都有自己的报告行。修复了 repair 会话 prompt 的一处渲染缺陷（字面 `$reasoning_clause`
发给了模型）与共享审核 `approve_tentative` 裁定在 schema 中缺失导致的通路失效。

repair 会话现在对每条整理候选给出显式裁定（采纳/不成立/需人工），写入持久化决策账本：已裁定的
候选不再每轮重复出现，模型拿不准又无法验证的项进 `knowledge report` 的「待人工裁定」清单，
用 `python -m finesub.llm.knowledge candidates --resolve <key>` 关闭。

### Agent 路径：联网检索预算放松、误判截断修复

Claude Code / Codex 等 agent 后端的联网检索预算放松：单任务上限从 8 次查询 / 64k 响应
token / 300 秒提高到 12 次 / 192k / 600 秒——实测单次搜索约 12k token，旧预算四次就顶到
响应上限而查询次数根本没用完。预算拒绝的提示语现在明确区分「这次被拒」与「检索失败」，
并带上已完成的检索计数，模型不会再把预算拒绝误读成「联网不可用」而丢弃已核实的结论。

修了一个会浪费配额的误判：agent 后端报告的是**会话累计** token 用量，harness 却拿它比对
单轮输出上限，健康的长会话会被误判为「输出被截断」并触发一次不必要的拆窗重跑（实测差
4,090 token 就会触发）。该启发式在 agent 后端停用，截断只认显式信号。三个 Claude 模型的
catalog token 上限同步修正（Opus/Sonnet 输出 128k，Haiku 200k/64k）。知识提案 hint 现在
自动带上本次调用真实检索过的 URL（取自 harness 台账，不再依赖模型自己复述）。

### 运行时指定 LLM 模型：`--llm-model`，不改配置文件

`finesub.pipeline`、`finesub.batch`、`finesub.llm.correction_translation` 与
`finesub.llm.knowledge` 的 `repair`/`verify` 新增 `--llm-model [任务组=]值`（可重复；裸值
= 全部任务组）。值可以是模型组或单个模型 target，对应任务组**整体换绑**——没有回退链，钉住的
模型接不了的调用会明确报错而不是悄悄换模型。只对本次运行生效，config.toml 不动；命令行优先于
配置里的 `[llm.preferred_targets]`（后者的值同样支持模型组了，语义同上）。knowledge CLI 原有的
`--model` 旗子改名为 `--llm-model`。

### 人声分离更快了，交付内容随之有可见变化

两处改动，累计 **1.11×**（270 秒素材 17.6→15.7 秒，33.6 分钟素材 58.0→54.4 秒，
RTX 5070 Ti）：编译包现在让 Inductor 在两个 Transformer 轴上benchmark Triton GEMM 模板
（长素材 +3.7%），块不再经磁盘往返而是在内存里交接（+6~12%）。**已有的编译缓存会自动重建
一次**，无需手动清理。编译包的缓存目录现在还带上了显卡型号——同架构的不同卡（比如 5060 Ti
和 5070 Ti）实测出的最优 kernel 不一样，换卡会重建一次而不是沿用旧卡的。

交付的 `-vocal.ogg` 内容会变，**时间轴不变**。三个原因，前两个是少了两代量化（块输入不再被
压成 16-bit，块输出不再多编解码一次 FLAC），第三个修了一处缺陷：**峰值超过满刻度的源不再被
削顶**。ogg/mp3 解码常有轻微过冲，此前那些样本会被削平，并且因为分离器按峰值定归一化增益，
一次瞬时过冲会把整个块的电平算错（实测一段 524 秒的块偏了 1.27×）。既有的 `-vocal.ogg`
不必重跑（它本来就随 worker 数变化而不可复现）。

### 修复：中文 Windows（GBK 代码页）下三处子进程调用会崩

`subprocess.run(text=True)` 用机器的 ANSI 代码页解码。在 cp936 上，ffprobe 吐一段 UTF-8
元数据（一个非 ASCII 的轨道标题就够）就会在读取线程里抛 `UnicodeDecodeError`——发生在调用方
拿到退出码之前，报错还指不到真正的主角。三处都修了：ffmpeg/ffprobe 探测、知识库的 git 调用、
装机时的模型下载子进程（同时给子进程钉上 `PYTHONIOENCODING`，两头都说 UTF-8）。
新增一条源码守卫，`text=True` 不带 `encoding=` 直接红。

### 修复：Windows 上发布性重命名被句柄占用就整个失败

每次装机发布都以一次 rename 结尾，而 Windows 在任一端还被打开时会拒绝它——杀软扫描刚解包的
`ffmpeg.exe` 就够，普通读者也够（Python 的 `open` 不共享删除）。此前只有 Python 运行环境的
启用交换有有界重试，另外十二处（下载发布、资源启用、目录搬运、原子写、任务索引、`.env`）是裸的，
于是几分钟的下载会因为一次通常不到一秒的拒绝而作废，界面上只留一行 `[WinError 5] 拒绝访问`。
现在统一走 `fsops.replace_path`：目录树/下载用长预算，记录类用短得多的预算。两处**故意**不重试
——`move_directory` 的跨卷探测（它的失败*就是*信号）和摘要不符后的隔离重命名。

### 修复：`cl.exe` 在 PATH 上但 `INCLUDE` 没配全时，分离器编译档位判断假阳性

只查 `cl.exe` 会把半配好的开发者 shell 当成可用工具链，于是用户被告知要跑 90 秒编译、然后
掉回 eager——正好是这个探测存在的理由。现在要求 `INCLUDE` 里真能找到标准头才算数；
vcvars 那一支不变（启用它本来就会配好 `INCLUDE`）。

### 新增：`GEMINI_BASE_URL` 可以改 Gemini 直连 REST 的端点

`.env` 里设 `GEMINI_BASE_URL=https://…/v1beta` 即可让生成调用走镜像或代理。此前两个自定义
provider adapter 都能配 base URL、唯独直连 Gemini 不能，这是不对称不是设计。
**只覆盖生成调用**：文件上传、`countTokens`、grounded 搜索仍直连 Google。

### 新增：`[llm.preferred_targets]` 一行指定优先模型

`config.toml` 里写 `[llm.preferred_targets]` + `default = "<target>"`（或按任务组给一行），
该 target 会被**放到**选中预设那格模型组的**最前面**，而不是替换它。保留原候选是有意的：
自定义 OpenAI-compat / Anthropic target 是纯文本的，替换会让音频/视频任务无处可去。
改这张表会作废 checkpoint（与其它路由改动同口径）。用法见 `docs/manual/model-routing.md`。

### 新增：VAD 成为独立阶段，产物 `<stem>-vad.json`

VAD 能量分段此前和 Whisper 绑在同一个产物里，删掉 `-aligned.json` 重跑识别/分句/复核就得连
VAD 一起重跑。现在它有自己的产物（`-vad.json` + `-vad-energy.npz` 帧级轨），按存在性复用，
音频或 `--vad-silero-assist` 一变就自动作废重算。两个文件都算可清理产物，跑完随其它中间件一起删。

### 约定：打包资源路径变更计入破坏性变更

`desktop/runtime/pylock.*.toml`、`desktop/resources/runtime-manifest.json` 这类被外部按路径
取用的文件，位置变了要在本文件写一条。不承诺路径稳定，只承诺变更被记录——这类文件事实上
被下游按固定路径直接取用，挪动等同于改公共接口。

### 知识库信号报告与共享（实验性）

- `python -m finesub.llm.knowledge.report`：只读报告——每个词条/字段的命中、曝光、落地计数与
  精修印证（确证/推翻、最近日期）、从未命中的别名/误听项、以及「反复曝光却从未落地」的高误触发
  候选（建议关闭精确匹配）。数据在纠错/知识更新运行中自动积累，失败不影响任务本身。
- `python -m finesub.llm.knowledge.share`（客户端）与 `python -m finesub.llm.knowledge.share.server`
  （本机/轻量服务器）：知识库共享的首个可运行形态——`mark`/`unmark` 显式勾选或撤销可共享的条目
  （人际关系、真实姓名类内容默认不共享，`--match` 可只勾单条）、`push` 进服务端审核队列（维护者
  逐条批准/合并/拒绝后才入库；断网重试不会重复排队）、`pull` 拉取合并（校验完整性链，本地改动
  优先，冲突列出来交给人）。共享文本双向按不可信输入处理，服务端入队前再清洗一遍。维护者侧
  `share review` 负责审核：默认只列门槛预检与审核 prompt，`--execute` 才调模型联网核实并逐条
  给出出处，`--post` 才回写裁定——审核模型与 API key 始终在维护者本机。服务器部署模板
  （systemd/Caddy/litestream + 过渡期 Cloudflare Tunnel 步骤）在 `deploy/share-server/`。

### 知识库换成 SQLite 真相源，不再需要 git

知识库根下多出 `knowledge.sqlite`（版本行、每次更新一个 revision、按实体 CAS），原来每条目一个
markdown + 内嵌 git 的形态在第一次打开时**一次性无损导入**，之后只作历史档案；`knowledge/rendered/`
下是每次写入后重生成的 markdown 缓存，**现在也是可编辑投影**：用文本或 markdown 编辑器改它，
下次纠错 run 启动时自动收割为一次用户修订（解析失败的文件告警跳过、原样保留）；条目行带 `- `
前缀，渲染态 markdown 编辑器里每条一行。`--knowledge update` 不再索要 git，`finesub doctor`
把 git 列为可选。知识更新的提案格式随之改为按句柄操作（`update`/`remove`/`retire_entry` 取代
`edit_lines`/`replace_section`/`delete_entry`，`PROMPT_VERSION` → v79）；词条结构随后改为三/四段
（`表面形|中文|描述[|别名]`——读音列取消并入别名项，「常被误听」改为仅供匹配器消费、不再注入
prompt，`PROMPT_VERSION` → v80），旧 checkpoint 按既有规则失效。`translation/` 下两个台账仍是
普通文件。人工维护走新 CLI `python -m finesub.llm.knowledge`：`edit` 把条目吐进 `$EDITOR` 改完
自动 diff 回库，`second-pass` 做老条目到新结构的确定式迁移并列出人工候选，`repair`/`verify`
（默认 dry-run）分别是候选修复与联网核证的 LLM 半，另有
`show`/`log`/`new`/`retire`/`revert`/`restore`/`refresh`。设计与实施记录见
`docs/plans/knowledge-node-plan.md`。

## [0.4.2] - 2026-08-26

### 修复：agy 项目记录的 `file://` 解析依赖于读它的机器

`finesub agent-clean --all-domains` 与 `uninstall --purge-big-data` 靠 agy 项目记录里的
`folderUri` 判断哪些记录归 FineSub。那段解析把「去掉盘符前的斜杠」挂在了 `os.name == "nt"` 上，
可是一条记录用哪种拼法取决于**写它的**机器，不取决于读它的——在非 Windows 上读一条
Windows 写的记录会得到 `/C:/…`，扫不中、记录留下。改为按字符串形状判断，同时不会把 POSIX
下合法的 `/a:b` 误当成盘符。

### 分离器多了一个工作采样率开关，并修掉一处会让长音频交付静默崩溃的缺陷

`--separator-rate {44100,32000,22050}`，默认 44100（模型自己的率，行为不变）。低两档按比例
减少块数——长素材上 22050 约 1.5×、32000 约 1.26×——代价是分离质量。**两档都不推荐**，
16000 不提供（它会整段抹掉人声）。取舍与全部实测见 `docs/separator-optimization.md` 的 E12。
分离阶段按存在性跳过且文件名不带采样率，**换档要先删掉已有的 `<stem>-vocal.*` 及其下游**；
run metadata 的 `workers.vocal_separation.sample_rate` 记着产物是哪一档做的。

同时修掉一处既有缺陷：写 16 kHz Vorbis 交付时，一次 `write()` 超过约 50 万帧会让 libsndfile
**进程级崩溃**（不抛异常，只留下一个 header 大小的空文件）。此前每窗恰好 320000 帧才没暴露；
现在写入按 262144 帧分块。

### 本地 agent 调用改为工具会话；传输由会话档位派生（破坏性）

Claude Code、Codex、agy 在出厂默认的 `per-window` 档下不再收整包 prompt，而是通过 harness 自己的
MCP server 取 task、读协议与正文、提交，并在同一会话内完成第一档修复（`--max-retries-per-window`）；
`--retrieval native` 在这条路上改走 harness 的本地检索代理（`web_search`/`web_fetch`）。`api` 与
`resume` 档、以及任何带音视频的调用仍走原来的 capsule 传输；CLI 探不到 per-invocation MCP 能力时
`per-window` 也回落 capsule 并提示一次。agy 会在 `%LOCALAPPDATA%`（或 `--agent-runtime-root`）下
多出 `.finesub-tool-<n>` project，并往这些 project **自己的**记录里写 `mcp(finesub/…)` 授权；全局
settings 不动。`finesub agent-clean --all-domains` 与 `finesub uninstall --purge-big-data` 会顺带
删掉 agy 为这些目录登记的 project 记录。

新档位 `agent_session = "pseudo-conversational"`（按任务组/难度设）：一次 run 里绑到同一 agent
模型的全部文本调用共用**一条长驻 CLI 会话**——背景调查、逐窗纠错、任务后知识更新都在里面——agent
交完一个 task 就向 harness 要下一个；带音视频的调用仍各自走 capsule。只有 CLI 能接 per-invocation
MCP server 时才可用，否则启动即报错。usage 按**会话**记账（CLI 一次调用只报一次总账），所以任务
报告 Provider Token Totals 里 agent 那行的 token 来自会话总账、调用次数仍按窗口算。四个档位现在写
在 `docs/manual/model-routing.md`「会话档位」一节。

把某个任务组绑到 `conversational-agent` 模型组，这一组的文本调用就会排队等**你自己正在用的 agent**来做：
运行时会提示一条 `finesub agent-join <目录>`，在你的 agent 里执行它、把打印出的说明交给 agent 即可；
每个调用最多等一小时没人来领就报错（固定值，见下面「超时收敛成一个键」那条）。这条路**只收纯文本**：
带音视频的格子别绑给它，那种调用会在该格直接报错停下，而不是悄悄把媒体丢掉发纯文本。

本机 agent 的工具会话**成功后不再留下现场**：拿到答案就删掉那次调用的 assignment 目录（里面是
整条 run 的字幕正文与每一帧 MCP 记录）。以前每次成功调用都会留下它并打一行「keeping assignment
root」告警——那是把「审计包写失败要留证据」这条规则用反了。失败、或整条 run 抛错时照旧整棵保留，
交给 `finesub agent-clean`。

**破坏性**：`[llm].local_agent_task_runtime` 与 `[llm].local_agent_tool_calling` 两个实验键已删除
（配置里写它们会被忽略，传输只由 `agent_session` 档位派生；开发者可用环境变量
`FINESUB_AGENT_TRANSPORT=capsule|tool-session` 强制）；`agent_session` 档位现在进 resume 身份，
改档位会让旧 checkpoint 失效。agent CLI 没装、装了但坏了、或缺本次调用所需能力时各提示一次
（以前静默跳过）。

### conversational：提交前可以自检，答案不用手工转 JSON，跑完留下记录

接着上一条，把首次真机实测暴露的其余几处收掉：

- **`finesub agent-task lint`**：用与 submit **完全相同**的校验器检查一份答案，但不消耗任何
  预算、不计修复轮、不改任务状态，可以反复调用。截断、少写几列、覆盖不全在提交前就现形，不必
  等一次 submit 失败再整篇重写。
- **`--text-file`**：`submit` 和 `lint` 都能直接读一个文本文件当作答案，不再需要把几十 KB 的
  内容手工转义成一个 JSON 字符串。原来的 `--json-file` 保留。
- **交给 agent 的说明书改对了**：`submit` / `lint` 的必需参数完整写出、给了可照抄的命令、点明
  「每条控制命令都会续租」和「进度可以 `checkpoint-progress` 存任意 JSON」；命令名按**这台机器
  实际能跑的**生成——源码安装没有 `finesub` 这个可执行文件，此前照抄必然失败。
- **成功跑完不再毁尸灭迹**：每个完成的窗口的协议、正文、答案和一份控制摘要会落进产物目录的
  `agent-conversational/`，和 API 调用留下的记录同一个位置；assignment 目录本身只留一个几 KB 的
  控制面，好让还挂在等待里的 agent 读到「这批活干完了」，而不是撞上一个消失的目录。归档由
  会话作用域负责，所以单独跑纠错阶段、或跑 `reference_ingest` 的知识更新，留下的记录一样全。

### 第四个本地 agent 后端：dsh（DeepSeek Harness）

`npm install -g @deepseek-ai/dsh`（要 Node ≥ 22.19），key 放 `~/.dsh/.credentials.yaml`，
在 FineSub 里绑 `dsh-capable` / `dsh-basic` 两个模型组之一。出厂**不绑**任何格子。
自己的 OpenAI 兼容网关写一行 override catalog 就能接，见 `docs/manual/model-routing.md`。

它比另外三家窄，而且是硬性的：dsh 只从命令行接任务，一整窗字幕塞不进去，所以**只支持按窗口
的工具会话**——绑到 `api` 或 `resume` 档位会直接报错说明原因，不会静默降级；不收音视频；
不能续会话；也**报不出 token 用量**（headless 只打印最终答案，没有任何统计可读），任务报告里
它那行的 token 数因此是空的。同样因为它不报工具调用，`--retrieval native` 的调用在报告里记的是
「无从判断有没有搜索」而不是「没搜索」——后者是替它下了它没下的结论。

已经拿一整窗真实字幕验过：270 条的生产窗口、DeepSeek-V4-Flash，782 秒一次跑完，无重试。
**别拿免费或限速的额度验收它**——同一窗在一条被限到 ≈5 tok/s 的端点上两个尺寸都跑到超时、
一个字都没出来（模型本身在正确干活，只是走不完），换成正常速率的端点就过了。

两条 key 相关的：dsh 的联网搜索由它自带的 DeepSeek 搜索插件提供，那个插件读**自己的**凭据
（缺省 ref 名 `DEEPSEEK_API_KEY`），和跑模型的 key 是分开的——可以拿别家 key 跑模型、只拿
DeepSeek 的 key 用来搜索。思考档位则只有 DeepSeek 自带路由由 FineSub 设（`medium` / `xhigh`
会自动译成它认的词）；自带网关的那一档留在你自己的 `settings.yaml` 里，FineSub 不碰——它的
配置覆盖是整键替换，替你写会把 baseURL 和 key 一起冲掉。细节见 `docs/manual/agent.md` 4.1。

### 本机 agent 的超时收敛成一个键（破坏性）

`[llm].local_agent_timeout_seconds` 现在只有一个意思：**一次调用最多跑多久**。默认从 900 秒
提到 **1680 秒（28 分钟）**，没有上限。此前它同时兼着三件互不换算的事，调它会在别处产生意外后果。

- **租约时长改为由它派生**：`一次调用 + 120 秒`。租约是"我们替这个 agent 把任务留多久"，必须
  严格晚于给 agent 的死线——否则一个**准时**算完的调用会在走到 `submit` 的路上把租约耗光，
  已经花掉的输出直接作废。三种会话形态（一次性工具会话、长驻会话、conversational）现在同一个
  来源，不会再走散；此前只有 conversational 从这个键派生，另两种钉死 30 分钟，而把这个键设到
  1680 以上会让 per-window 工具会话直接拒绝开工（校验范围却一路放行到 3600）。
- **conversational 等人来领不再是配置**，固定一小时。它唯一的职责是别让 run 无限挂着；想晚点
  再到场，正确做法是手册里的分两步跑（先跑完语音部分，人到场了再跑纠错），比让 run 空等强。
- **接手一次就把等待时长重新计满**。此前累计不返还，于是 agent 会话中途掉线、人重新 `agent-join`
  时可能发现任务已经被撤——尽管几分钟前明明有人在做。反复接了又走由次数管：同一个任务被领
  **3 次**仍未完成就放弃，报错直说是这个原因。

### conversational：等人的表和干活的表分开，`agent-join` 不用再记目录名

把任务组绑给**你自己正在用的 agent** 那条路（`conversational-agent`）有三处改动：

- `local_agent_timeout_seconds`（默认 900 秒）现在只算**没人来领**的时间。此前它是从入队开始的
  一把总墙钟，于是一个已经接了活、每条 `finesub agent-task` 命令都在续租的 agent，也会在到点被
  中途撤单——想让它慢慢做就只能把这个旋钮顶到上限。现在有人在做的时候这个表就停着；对方长时间
  完全不发命令、租约过期、任务退回队列之后，它才从断点继续走。配套地，这条路的租约时长跟着
  `local_agent_timeout_seconds` 走（不短于 30 分钟）——它是**静默容忍度**，要覆盖得住一次长生成。
- **`finesub agent-join` 可以不带参数**：它自己找到那个在等的 run，多个在等就列出来让你指名。
  那行提示只在第一次入队时打一次、而目录名每次运行都不一样，错过就再也找不回来；现在错过也能
  接上。提示本身也不再是"打一次没打出去就永远没有了"。
- assignment 目录名与 assignment id 统一成同一个 `conv-<hex>`（此前是两个不相干的随机串），
  并且落到与本机 agent 现场同一个目录下——**一条普通 `finesub agent-clean` 现在清得掉**。此前它
  在清理命令的射程之外，一次失败的 run 会把整篇字幕正文永久留在临时目录里。

### 修复：CLI 运行的设置记录写错了两个开关

任务记录里存的是「这次运行实际用了什么」，桌面端据此重试或续跑。`--llm-difficulty` 的取值表停在
2026-08-12 改名前的 `high` / `med` / `minimum`，于是现在的合法取值一个都对不上——`efficiency` 会被
记成 `high`，回读时再被折算成 `quality`，重试跑的是另一档难度；`--llm-media` 缺省则记成 `video`
而管线的缺省是 `audio`，命令行没写这个开关时同样记错。两处都已按管线的实际取值表与缺省改正。

### 三处「一次瞬时故障就得手动重跑」的修复

- **分离器 JIT 编译失败不再中止任务。** `torch.compile` 在第一个 block 才真正编译，失败（如
  编译缓存损坏）此前直接终止分离。现在编译被提前到任务开始前的一次 warm-up；失败则原地退回
  eager 继续，并记下判定让下一次运行不再重付编译（显存不足除外）。想重新启用编译加速就删
  `accel/<key>/` 目录。JIT/AOTI 安装到一半失败也会把模型还原干净，不再出现「报 eager、实为
  半编译」。run metadata 新增 `accel_requested` 与 `accel_fallback_reason`。
- **Gemini 媒体上传自带重试。** 上传途中的连接重置（`[WinError 10054]` 这类）最多重试 3 次
  （新建会话整体重传），与模型调用的重试预算分开；确定性的 4xx 仍立即失败。
- **`fine-metadata.json` 被短暂占用时自动重试**约 2.4s；仍失败时错误会列出可能的持锁者
  （另一个 finesub 实例、杀软/索引器、编辑器、无写权限），不再是一句裸 `拒绝访问`。

### ⚠️ 破坏性：纠错窗口的重试预算改为两档，最坏成本上界变大

此前一个窗口只有一个计数器：`--max-retries-per-window`（默认 5，含首次最多 6 次调用），
整条重试链喂给同一条会话——会话一旦退化（复读、认死一条错读法），剩余预算全部白烧。
现在预算分两档：

- `--max-retries-per-window`（默认仍 5）改为**一条会话链内**的修复次数——每次重试带上一轮
  输出与校验错误，agent 后端续用同一会话；
- 新增 `--max-replacements-per-window`（默认 1）：链用尽后把窗口交给**全新会话**再来一轮
  （修复上下文丢弃；agent 开新会话，纯 API 后端即盲重掷）。

一个窗口的调用总数是两个 (n+1) 的**乘积**：出厂默认从最多 6 次变为最多 **12** 次。典型路径
（一两次修复即通过）成本不变；要恢复旧上界，设 `--max-replacements-per-window 0`。
`finesub.batch` 的 manifest `opts` 同名新键一并可用。

配套地，`--continuity parallel` 的熔断改成计 **3 次会话链耗尽**（原为 3 个窗口失败）。
两档之后一个窗口要烧满 12 次调用才算「失败」，按窗口计等于让熔断的价码随第二档一起翻倍——
实测系统性失败下从 24 次调用涨到 48 次，而它要防的免费 flash 日额度是 20。按链计与改动前同价
（单 lane 实测恒为 3×6=18 次）。此外熔断触发后，**在飞的窗口现在于下一次 attempt 边界停手**，
不再跑完剩下的预算；已经通过校验的窗口照旧进缓存，被截断的一定是至少失败过一次的窗口。

### 修复：`--continuity parallel` 在第一把 API key 耗尽后够不着 key 池的其余部分

并行为了保住「任意时刻只有一把 key 有在途请求」这条不变式而钉 key，但钉的实现是把
key 列表截成第一把，且截断发生在「跳过已耗尽的 key」**之前**。于是第一把 key 的当日
额度一用完，并行任务就报「所有 key 都被跳过」，而池子里下一把 key 额度还是满的。

现在 `--continuity parallel` 在 key 这件事上与 serial 完全一致：不再额外钉 key，配额错误
先在同一把 key 上 sticky 重试，把该 key 的重试预算花光才轮到下一把。

**带媒体的调用仍然钉一把**，但钉的是**文件自己那把**：`UploadedFileRef` 现在记录实际上传
它的 canonical key id 与所属 tier，发车前按当前候选核对一次；owner 不在候选 tier 的当前池中，
或那把 key 已锁，就换一把能服务的重传，调用再钉到新的那把。这连带修掉一条更隐蔽的路径——
生产默认在切片时就把 clip 传掉了（那时还不知道会路由到
哪个模型），一旦那把 key 当日锁定，之前每个候选都会拿一个读不到的文件去请求，403 又是
不可重试的 4xx，整条候选链走完失败，而池子里其余 key 额度还是满的。上传缓存同理按当前模型
复查（缓存按 `(tier, 文件)` 建键，而锁是按 `(tier, model, key)` 的）；FREE → PAID 候选切换
也会因 owner tier 不同而重传，不再拿 FREE 项目的 Files object 给 PAID key。串行路径逐字节不变。

### Agent 会话模式旋钮重定义为四档并真正生效

`agent_session_mode`（`model_routes.toml` 预设的 `agent_session` 表）此前三个值且无人读取；
现在四档并由生产调用路径读取：`api`（逐调用新会话）/ `per-window`（**默认**，即原有行为：
一个窗口的修复链续同一条会话）/ `resume`（实验性跨窗复用，默认关）/ `pseudo-conversational`
（仍拒绝）。旧值 `per-session` 不再合法，配置里写它会在路由表加载时报错——改用 `api`
（同语义）。`resume` 下跨窗继承会话的调用不再写入 session checkpoint（恢复时该次调用重发），
详见 `docs/llm_local_agent.md` §12.1。

### 新增：`--lang-redecode`——语言票翻转窗口的强制语言重解

日语等 CJK 直播里偶发的「连续几段输出英文、真实台词整段丢失」现象有了修复开关：
`vad-asr` 阶段（及 `python -m finesub.pipeline` 同名透传）新增
`--lang-redecode {auto,on,off}`，默认 `auto`（依赖可用即运行）。当一个解码窗的检测语言与近期
窗口的众数语言不符时，用 Qwen3-ASR 第二模型对该窗的 VAD 区间重认取证，并以众数
语言强制重解该窗；只有证据支持新结果时才替换，任何存疑情形都保留原结果。开关
关闭时输出与旧版逐字一致。它只覆盖「group 语言票与近期众数翻转」的崩塌；
检测语言仍正确的同脚本崩塌不在首版触发面。阈值仍待真外语素材标定，
机制、取舍与实测见 `docs/asr-align.md`「语言票翻转重解」。ASR 断点指纹加入本开关，
新旧 partial 不互相续跑，重跑即可。`--qwen-verify` 保持原有三类嫌疑与探测跨度不变。
### LLM 纠错阶段不再是一段静默

此前从进入纠错到写出成品之间只有首尾两行，跑多久都看不出在哪一步。现在它按窗口上报进度，
过程细节进 verbose 日志。**分母会变大**：一个窗口重试时会被拆成更小的几个，
拆出来的部分是真的多出来的活，所以"12/40"之后出现"12/43"是实情而不是错乱。

运行摘要补上"修复轮"与"被内容过滤拦下"两个计数；内容过滤真的丢掉了内容时会明确告警，
而不是只在阶梯内部悄悄降级。

### 模型下载：CLI 也有镜像回退，下载完会校验

- 镜像失败时按模型回退到官方源。此前只有桌面端有这条回退，CLI 是"哪个库要就哪个库下"，
  镜像不好使就直接失败。下载仍在**用到它的那个阶段**触发，不提前到装机。
  只对默认模型生效：`--model` 指了别的模型时不会先多下一份默认模型。
- 下载完成后按清单摘要校验一次并留下标记，校验过的不再重复校验。此前的失败方式是
  "报告装好了，第一个任务才发现不对"。校验失败同样留下标记并清掉坏文件，下次运行会重新
  下载，而不是把已知损坏的权重当就绪；加载时锁定校验过的那个版本，不受仓库后续更新影响。
- 镜像发回内容错误的文件（长度对、哈希不对）时，校验失败当场记为镜像故障并立即转官方源
  重取，官方源的结果同样要过校验——此前这种情况只会反复失败在同一个镜像上。

### GPU 解码不再依赖「torch 恰好先被导入」

CTranslate2 在运行时按名字加载 `cublas64_12.dll`，而我们的 wheel 不带它。此前它能被找到，
只是因为 torch 先被导入过、顺手把自己那份的目录加进了搜索路径——阶段顺序一变就断，且断了
没有征兆。现在在建模型前主动去找（不导入 torch），而不是从 C++ 里抛一个看不懂的加载失败。

本项目**有意**钉在 CUDA 12（CUDA 13 兼容面窄得多）。所以如果你把 torch 换成了 cu13x 构建，
告警会直接点名「只有 `cublas64_13.dll`」并让你换回 cu12x，而不是含糊地说找不到文件。

### 任务被占用时说得出是谁

- 桌面端对一个仍在跑的任务点"继续"/"重跑"，此前是任务起来又立刻消失；现在动作发生前
  就拒绝，并说明是哪个前端占着（pid、开始时间，跨机器时还有主机名）。
- `finesub relocate` / `finesub uninstall` 因为"有东西在跑"而拒绝时，同样带上持有者。
- `finesub doctor` 多一行 `activity`：几个运行实例在活动、哪些任务被谁占用、有没有安装在进行。
  它检查的正是上面两条命令会拒绝的那些条件，不会出现「doctor 说空闲、relocate 说在跑」。

## [0.4.1] - 2026-08-18

### ⚠️ 从 0.4.0 升级请用 Setup 覆盖安装，不要用应用内更新

**0.4.0 的应用内更新走不通**，两条路都不行，而且这两处都在 0.4.0 自己那半，
0.4.1 无论怎么改都够不着——应用内更新用的是**你机器上已装的**那套更新程序：

- 增量更新装完后应用起不来（0.4.0 冻结的组件读不懂本版新增的 ffmpeg 下载配置
  形状）。会连续两次启动失败后**自动回滚**回 0.4.0，数据不受影响，但更新不生效。
- 完整更新根本没应用上（见下一条的根因），安装保持 0.4.0 可用。

**正确做法**：下载 `FineSub-Desktop-0.4.1-Setup.exe` 直接覆盖安装。已实测：
设置、API Key、知识库、成品字幕、模型与缓存全部原样保留，装完即认作 0.4.1。

0.4.1 之后的版本恢复正常，应用内更新与增量都可用。

### 修复：应用内更新从来没有等过应用退出

更新程序被拉起后**立刻**开始替换程序文件，而此时应用还在运行，于是它跟自己抢，
十秒后放弃并报「拒绝访问」——用户往往还没来得及关闭应用。它本该等最多一小时，
实际等了 0 秒：判断「应用是否还活着」用的方法在 Windows 上对**父进程**永远回答
不了，而那个回答被当成了「已经退出」。

现在改为等待应用真正退出后再动手。这也解释了为什么此前的完整更新从未成功过。

### 修复：全新安装拿不到 ffmpeg（以及 0.4.0 的 ASR 环境装不上）

两个装机链上的下载地址失效，症状都是首次运行环境安装中途报错：

- **patched CTranslate2 的 wheel 从未发布**。0.4.0 引用的
  `ct2-4.8.1+finesub0.4.0` 这个 tag 不存在，所有前端首次装 ASR 环境都会在这一步 404。
  已补发；**已经装好的 0.4.0 不需要升级或重装**，那个地址是装机时才解析的，重试即可。
- **ffmpeg 钉的构建被上游删了**。BtbN/FFmpeg-Builds 只保留几天的滚动构建，manifest 里钉的
  那个已经不在。

第二个换了做法而不是换个新地址：ffmpeg 现在跟随上游的 `latest`
（FFmpeg 9.0 发布分支），**校验没有取消**——安装时先向 GitHub 询问该文件当前的 sha256，
再据此校验下载内容。代价是不同时间安装的机器会拿到不同的 ffmpeg 构建。

顺带修好一个断点续传的隐患：暂停后隔天再续传，如果上游文件在这期间变了，以前会把新文件的
后半段接到旧文件的前半段上，下载完才校验失败；现在会认出目标变了并重新开始。

### 人声分离的交付分两个模式，管线用的那个改成 16 kHz 单声道

读人声轨的每一个下游（VAD、识别、Qwen 裁判）第一件事都是下混单声道 + 重采样到 16 kHz，
而我们此前交付 44.1 kHz 立体声——四分之三的码率花在了下一步就被丢掉的样本上。现在按输出
后缀分两个模式：

- `.ogg`（管线默认）：**16 kHz 单声道**，Vorbis 质量档从默认调到更保守的一档。同素材实测
  **355 KiB / 28.6 dB**，对比此前 **719 KiB / 23.97 dB**——体积减半，识别真正读到的那段
  频带反而干净了 4.6 dB。
- `.flac`：无损交付，保持模型自己的采样率与声道数，全链零有损。试听、测量、做实验用它。
  只能通过直接调用分离模块得到（`-o x.flac`），管线不会产出。

其它后缀会直接报错，不再默默按后缀猜格式。

既有的 44.1 kHz 立体声 `-vocal.ogg` 照读不误，不必删；重跑才会拿到新形态。

同时修好一处：目录里只有 `-vocal.flac` 时，管线此前会当作「没有人声轨」再跑一遍分离，
尽管后面的阶段本来就能直接读它。现在两种交付任一存在都算这一步已完成。

### 人声分离的产物不再被有损编码两次

分离按块进行，每块先由分离器写成临时文件，再合并成最终的 `-vocal.ogg`。此前临时块用的是
和最终产物一样的格式，于是这段人声被 Vorbis 编了两次。现在块固定用 FLAC，最终产物格式不变，
少掉的那一代损失直接落在 VAD/ASR 读到的音轨上。

实测（60 秒素材）：相对完全无损的参考，人声轨信噪比从 19.9 dB 提升到 23.4 dB，改善集中在
ASR 实际使用的 8 kHz 以下；时间轴逐帧不变，文件大约 4%。

已有的 `-vocal.ogg` 不必删——它仍是一段有效人声轨；重跑才会拿到新的。

### 本地视频输入不再在人声分离前被有损压一道

以前输入本地视频（`.mp4`/`.mkv`/…）时，管线会先用 ffmpeg 抽一份
`<stem>-source.ogg`（Vorbis q5 + 下混单声道 + 重采样 16 kHz），再拿它去做人声分离。
这是全流程里唯一一次由我们自己引入的有损压缩，而人声分离用的是 44.1 kHz 立体声模型，
等于先把信息丢掉再让模型去还原。

现在本地视频和 URL 视频走同一条路：源媒体原样交给各阶段，只有当 soundfile 打不开容器时
才由分离阶段解一份**无损** FLAC（保持采样率与声道数，跑完即删）。`-source.ogg` 这个产物随之
消失，清理逻辑也不再找它——旧任务目录里遗留的同名文件需要自己删。

知识库素材导入（`finesub.workflows.reference_ingest`）的 `media=video` 任务同理：下载的
`<id>.mp4` 现在直接作为 pipeline 输入，不再先抽一份 `<id>.ogg`。

## [0.4.0] - 2026-08-17

### 每次运行都会存一份详细日志

出问题时不用重跑一遍加 `--log-level verbose` 了：每次运行都往
`%LOCALAPPDATA%\FineSub\user-data\logs\run-<时间戳>-<输入名>.log` 写一份完整记录，终端显示的
内容不变。

日志只记 FineSub 自己的判断，不含第三方库的刷屏和进度条，所以一次运行通常只有几 KB。和桌面端
的安装/会话日志共用「保留最新 100 个」这一条规则，不会一直堆积。

**这一版覆盖的是识别侧**：选了哪块显卡、各阶段耗时与显存、跳过了哪个阶段、异常片段在救援阶梯的
哪一级被隔离以及判据。**纠错翻译那一步目前只记开始和结束**——它的细节仍在任务产物目录里
（`task-report.md`、`correction-windows.jsonl`、`exchanges/`），报障时请连那个目录一起提供。

### 破坏性：Python 包改名 `asr_playground` → `finesub`，`llm` 并入其中，命令行只留 `finesub`

**只影响直接用 Python 包或仓库开发版的人**。桌面端与 `finesub` CLI 的用法、参数、
产物、任务记录一律不变。

- **import 路径**：`asr_playground.*` → `finesub.*`，顶层 `llm.*` → `finesub.llm.*`。
  后者顺带消掉一个通用名——装上本项目后 `import llm` 不再是我们的包。
- **仓库根包不再提供 console script**。`asr-pipeline`、`vad-asr`、`vocal-separation`、
  `asr-align`、`asr-stabilize`、`vad-energy`、`to-srt`、`llm-correct-translate`、
  `llm-knowledge-update`、`llm-reference-ingest`、`llm-token-compare`、
  `finesub-agent-clean/ping/task` 全部删除，改用 `python -m finesub.<模块>`
  （写法见 `docs/manual/repo-install.md` 与 `README_DEV.md`）。对外命令只剩一个
  `finesub`，来自 CLI wheel——它会准备托管运行环境，而根包的那些不会，两者同时出现在
  PATH 上只会让人踩坑。
- `python -m finesub.speech.recognition.cli.align` 与 `...cli.vad_asr` **此前没有
  `__main__` 守卫**，`python -m` 会一声不响地成功退出。已补上，并加了守护测试。
- 仓库目录名（`asr-playground`）不变；CHANGELOG 里的历史条目保留旧名。

> 维护者注（0.4.0 发版演练发现的四件事）：
> ① 0.3.x 冻结启动器按 `src/asr_playground/pipeline.py` 校验载荷并定位应用源，
> 0.4.0 起载荷携带 asr_playground 占位文件（`package-bootstrap.ps1` 生成、
> `build_release.py` 把关）。② bridge 冻结在 exe 里、app 增量不换 exe，0.4.0 新增
> `get/save_preferences` 后 0.3.2 增量混血会静默丢设置持久化——增量只留给不动
> 冻结层的版本。③ full updater 搬程序文件改为带退避的纯 rename：旧实现一次
> PermissionError 即中止,copy+rmtree 回退还会把 `_internal` 删残。④ 发货的 0.3.2
> 序列化的 preserved 名单没有 `tasks`/`locations.json`，其应用内 full 更新会把成品
> 字幕挪进日后被清理的 backup——**因此 v0.4.0 不发 update-manifest**：旧版在应用内
> 看不到本次更新，迁移一律走 Setup 覆盖安装；updater 同时把自身名单设为地板与
> request 取并集。下一版本恢复 manifest。

### 没有显卡也能跑完，以及说清支持哪些显卡

**CPU 回退不再跑完就卡死**：没有可用显卡时，语音识别此前会正确算完、把中间结果写到硬盘，
然后**永远停在那里**——进程既不继续也不退出，字幕永远等不到。有位用户的 GTX 1060 因此白花了
九分多钟。原因在我们自己编的 CTranslate2 里：它选的 CPU 计算后端在释放模型时会死锁。换成
另一个后端后，CPU 路径不但能正常结束，还比原来**快了一倍**（同一段音频，识别 28.2s → 13.1s），
内存占用也略低。字幕内容逐字不变。

**显卡支持范围写进 README 了**，用型号说，不用「计算能力」这种查不到的说法：RTX 50/40/30/20
系、GTX 1660、GTX 1650 及更新（显存 ≥4GB）可用；GTX 10 系及更早不支持。

**不支持的显卡会自动回退 CPU，而不是报一个看不懂的错**。老显卡此前的表现是：驱动装得好好的，
torch 也说显卡可用，然后在真正开始计算时抛出 `no kernel image is available for execution on
the device`——人声分离阶段就崩了，根本到不了识别。现在会明确告诉你是哪张卡、它的计算能力多少、
需要什么型号，然后转 CPU 继续跑完。

**`--device` 只接受 `cpu` 和 `cuda`**：`cuda:1` 这样的写法此前会被接受但并不真的选到第二张卡，
现在直接拒绝并提示用 `CUDA_VISIBLE_DEVICES` 指定显卡。多显卡机器请改用该环境变量。

> 维护者注：patched CTranslate2 wheel 的 CPU GEMM 后端由 Ruy 换成裁剪过的静态 oneDNN，
> wheel 自带 Intel OpenMP 运行时，label 改为 `4.8.1+finesub<产品版本>.cu128`
> （运行时校验的判据相应改为只匹配 `finesub`，不含版本号）。中途试过的 MKL 已否决：
> 多吃 3.4 GB 内存且默认只在 Intel CPU 上启用。三后端实测与换 wheel 的最小验收见
> `tools/wt_refine_port/ct2-patches/README.md`。

### 字幕长度旋钮与设置持久化

**断句长短可调**：新增 `--split-length-scale`（0.6–1.6，默认 1.0，越小字幕越短），也可以
写进 `config.toml` 的 `[segmentation] length_scale`，桌面端「设置 → 字幕断句」有三档。它
缩放的是「多长算太长」的上限，不动下限——太短来不及读不是口味问题；已经合适的字幕不会
因此被切开。在 14 窗人工金标准上标定过：默认值不变，低于 0.8 只会多切不会切得更准。

**桌面端会记住你的选择了**：主题、语言、关窗行为、「别再问我」和处理设备此前存在网页缓存
里——安装版的网页缓存标识每次启动都不同，这些设置实际上**连一次重启都活不过**（0.4.0
发版演练实测）。现在统一存进 `user-data\settings.json`，随 `finesub relocate` 搬迁、和
卸载策略一致；任务表单也会记住上次用的模型、语言、显存档等选项。共享给命令行的设置仍写
`config.toml`，且**保留你手写的注释与排版**。

### 一轮全项目审查带来的修复

**字幕不再被静默改坏**：模型返回空答复或只有表头时，那一窗字幕此前会被当成「合法的空
结果」整段抹掉；答复少一列或多一列时，解析器会挪动列去凑，把置信度当成译文、把字数当成
时长写进成品。两处现在都按位置严格解析，对不上就判失败重试——宁可重来一次，也不要一份
看起来正常、内容已经错位的字幕。

**任务**：退出程序会一并终止正在跑的任务（缩到托盘不会），任务留在「已中断」，继续时
流水线跳过已完成的阶段；重试不再另起目录，产物仍在原处；历史页的点击不会再影响正在跑
的任务；某些可选组件缺失不再挡住所有任务。

**更新**：安装到一半被中断（关机、强杀）后不再留下一个打不开的版本——下次启动会自动
把上一版恢复回来，并把失败原因显示出来，而不是静默退出。

**桌面端选「最终字幕」不再崩**：桌面端一直在往纠错翻译传一个它已经不认识的档位值，
于是只要输出选到「最终字幕」，识别跑完之后就会在最贵的位置报错退出。只做识别的任务不受
影响，所以这个问题一直没被发现。

**任务历史里的「打开文件夹」不会再失效**：0.3.2 把任务产物从个人数据目录搬到了大文件目录，
搬迁有三处缺陷——中途被打断时历史里的路径不会被补写、便携版的产物在合并后仍留在原处、
以及从便携版带过来的历史记的是搬迁前的旧路径。任一情况下这些任务的「打开文件夹」都会指向
一个不存在的位置（文件本身一直都在）。已修，升级后自动补上。

**搬迁与卸载**：`finesub relocate` 跨盘搬迁时，你自己用 `mklink /J` 做的目录联接会
原样保留、重新指向同一个位置，不会把目标数据复制一份到新盘；卸载同样不会跟着联接删到
FineSub 目录之外。搬迁的新位置在开始搬之前就已登记，中途断电也能接着搬完。

**配额与网络**：请求过大之类的错误不再被误判为「配额用完」而白白冷却一把 key；每日配额
的计数跨日会正确重置；联网搜索拿不到某条结果时如实报告失败，不再把没拿到的内容当成
成功结果往下传。

### 历史记录里可以单独清理一个任务的中间产物

历史页每条任务多了「清理中间产物」：删掉人声分离音频、对齐结果这些占地方的文件，字幕和
识别结果（`*-stable.json`）保留，下次重跑仍然能跳过已完成的阶段。**跑挂或中断的任务也能
清**——它们才是最容易留下一堆大文件的（任务没跑完，自动清理根本没轮到），而且断点会保留，
「继续」照常可用。

### 不会再覆盖你自己的字幕

桌面版把字幕写在素材旁边，用的是固定名字（`视频名-raw.srt`）。如果那个名字已经被你手改过
的、或别的软件生成的字幕占着，它此前会被直接覆盖。现在只有**本机 FineSub 自己写过的**文件
才会被覆盖（重跑同一个视频仍然落回同一个名字，不会攒出一堆副本）；不是我们写的就改落
`视频名-raw.finesub.srt`，再冲突就往后编号。

### 临时文件不再落在「程序当时所在的目录」

纠错时每个窗口切出的音视频片段此前写在 `tmp/llm-audio-clips/`，URL 任务的「链接→视频 id」
对照表写在 `data/reference/`——**都是相对于启动程序时所在的目录**。装好的桌面版从自己的
程序目录启动，于是这两样东西落进了下次更新就会被整个替换掉的位置：片段没人清理，对照表
更新一次就没了。

现在片段放进该任务自己的产物目录（任务清理时一并删除），对照表放进用户数据目录（跟着
`finesub relocate` 走、和卸载策略一致）。仓库运行方式不变，仍是 `data/reference/`。

### 仓库运行方式：三条 `python -m` 命令换了模块路径

只影响**从源码 checkout 直接跑模块**的人；`finesub agent-clean` / `agent-ping` /
`agent-task` 这三个命令本身没变，桌面端与 CLI 壳也没变。

| 旧 | 新 |
|---|---|
| `python -m llm.agent_cleanup` | `python -m llm.agent.agent_cleanup` |
| `python -m llm.agent_ping` | `python -m llm.agent.agent_ping` |
| `python -m llm.agent_task_control` | `python -m llm.agent.agent_task_control` |

> 维护者注：`llm` 顶层的模块按职责收进了两个子包——`llm.routing`（模型事实/路由/执行身份/
> 开关轴/`config`/`api_keys`，层次方向由 `test_import_boundaries` 固化）与 `llm.agent`
> （本机 agent 后端）。`model_catalog.psv` 与 `model_routes.toml` 跟着进 `llm/routing/`，
> `pyproject` 的 package-data 键相应改成 `"llm.routing"`。纠错窗口循环
> `llm/stages/correction_loop.py` 拆成 `llm/stages/correction/` 八个模块。所有 import 路径
> 都变了；不做兼容别名。

### 模型事实表：两列改了名（自定义覆盖表需要改一行）

`model_catalog.psv` 的 `litellm_model` 改名 `api_model_id`、`model` 改名 `display_name`。
前者早就不经过 litellm，后者与「模型」这个词在别处的含义打架；新名字直说这一列是什么——
一个是请求里发给端点的模型名，一个是给人看的显示名。

**只影响自己写过覆盖表的人**：把表头那一行的两个旧列名改掉即可，底下的格子不用动。仍用
旧名启动会直接报错并给出新旧对照，不会静默忽略——一列被忽略的后果是「这个模型不在表里」，
离病灶太远。同时，改名会让 routing digest 变化，已有的纠错断点缓存作废、需重跑。

## [0.3.2] - 2026-08-06

### 桌面端：先识别、后纠错翻译，不再从头重跑

此前每个任务都写进独立目录，先跑一遍「原始字幕」、再对同一文件跑「最终字幕」时，
人声分离和识别会整个重来。现在复用只需把新任务指向旧任务的输出目录，流水线按产物
存在性自动跳过已完成的阶段（这套机制本来就在，桌面端只是从未给过入口）：

- 历史页里已完成的原始字幕任务多了「继续纠错翻译」——一键回到新任务页，识别结果
  已锁定复用，补充信息沿用当时填的（表单里已有的优先），确认后直接进入 LLM 阶段。
- 新任务页选了「最终字幕」而该文件恰有完成过的识别任务时，会主动提示可复用，一键
  接受，也可以无视它从头跑。
- 复用状态有明确的横幅说明（识别相关设置不再生效）并可随时取消。只有「原始字幕」
  任务可以被复用——已出最终字幕的目录若被复用，LLM 阶段也会因产物已存在被跳过，
  等于原样重发旧字幕。
- 顺带修了一个隐患：取消任务后换一个文件再开始，新任务会写进被取消任务的目录。

### 桌面端：新任务设置分成「语音识别」与「纠错翻译」两个标签页

处理选项按流水线的两半重新分组：识别语言、Whisper 模型、显存预算归「语音识别」；
补充信息、知识库归「纠错翻译」。输出结果（原始/最终字幕）仍在标签页之上——它是决定
LLM 阶段是否参与的总开关。选择原始字幕时纠错翻译页置灰但可见，页内一键切回最终字幕；
缺少 API key 时该标签上有小圆点提示，说明文字在页内。顺带删除了高级设置里残留的
per-task 处理设备下拉——设备选择全局化之后它已不再生效，留着只会误导。

### 桌面端：设备选择、可复制的界面、安装日志、真实进度

**处理设备**：机器上有多张显卡时，设置页可以指定用哪张（也可以选 CPU）。选择通过
`CUDA_VISIBLE_DEVICES` 生效——下游只看得见那一张、编号为 0，因此分离器/ASR/校验三个
消费者一行不用改，显存档位的假设也仍然成立；同时固定 `CUDA_DEVICE_ORDER=PCI_BUS_ID`，
否则 CUDA 按「最快优先」排序，选单里的 1 号和实际跑的可能不是同一张卡。显卡清单用
`nvidia-smi` 在后台线程探测，界面只读结果、从不等待；选过的卡不在了会自动回退并在任务
日志里说明（重跑/续跑同样覆盖）。

**界面文字可选中复制**：此前 pywebview 注入的全局 `user-select: none` 让整个应用无法
选中——报错信息、路径、日志只能手抄。现在除标题栏与按钮外都可以正常选中复制。

**安装日志自动留存**：安装运行环境时的完整输出写入 `user-data\logs\`，逐行落盘，成功、
失败、暂停三种结局都会记一行结果；保留最近 100 份，资源页可一键打开该目录。此前只有
内存里的最后 100 行，关掉窗口就没了。

**任务进度不再一开始就跳到最后**：worker 过去在任务刚开始时就上报「目标阶段」，界面把
它当成当前阶段，于是前几步立刻打勾、最后一步一直转圈，全程再不更新。现在由 pipeline
在进入每个阶段时上报，复用已有产物的阶段显示为「已有结果」而不是刚跑完的勾。

### 桌面窗口：原生缩放、标题栏行为与主题动效

无边框窗口重新可以用鼠标拖边缩放：加回 `WS_THICKFRAME` 并用 `WM_NCHITTEST`
子类化自己判定边框与标题栏区域，安装在 WinForms 的 UI 线程上。pywebview 的启动
回调早于 WinForms 窗口存在，所以这些工作等 `shown` 之后再做；等待超时只打
`Warning:` 并继续绑定文件拖放，不再让整个回调抛异常。最小尺寸 900×620 → 720×520。
标题栏双击最大化/还原，最大化按钮改为切换。

原生边框与标题栏配色跟随应用主题（新增 bridge 方法 `set_window_chrome`，前端在
每次外观变更后回传当前 `--app-bg`/`--text`）；窗口刚创建、前端还没画出来的那几帧
按 Windows 的浅色/深色设置取色，默认的「跟随系统」主题因此不会闪烁另一半配色。

动效：侧栏切换的移动指示条、主题色切换的 View Transition、任务高级选项的展开。
全部是 CSS/浏览器原生能力，没有引入动画库；设置页可整体关闭，也尊重系统的
`prefers-reduced-motion`。设置页的「查看文档」改为应用内对话框。

安装器：简体中文不再取决于编译机上有没有装社区译本（Inno Setup 官方不含简中，
此前的探测在 CI 上一直落空，发出去的其实是英文界面）——译本随仓库分发。英文保留，
按系统 UI 语言自动选择，不再弹语言选择框。

### `.env` 密钥保护：绑定 Windows 账户

`.env` 里的 API key 不再以明文落盘：随机主密钥经 DPAPI（绑定当前 Windows 账户，带
应用 entropy）包裹后存在文件首部的 `FINESUB_KEYRING` 行，每个密钥值原地替换为
`fs$…` 密文（信封加密，纯 stdlib）。变量名、命名 key 的显示名、注释与换行逐字节
保留，`cat .env` 仍能与 `config.toml` 的 `[pools]` 对照。转换由启动迁移
`0004-protect-env-keys` 完成（源码 checkout 则在首次读取时兜底），失败一律退回明文
加 `Warning:`，绝不阻塞运行。

对外措辞统一为**绑定当前 Windows 账户的保护 + 防泛扫混淆**：它防误传文件、通用
扫盘、DPAPI 批量收割、同机其他账户，不防以当前用户身份执行的代码（程序必须能无
口令自解密）。密文随 `.env` 拿到别的机器上表现为「未配置」且**一个字节都不动**；
只有在用户显式重填/删除全部不可解密的值之后，保护才会以新机器的账户自动重建。

导出通道：`finesub keys`（默认掩码，`--reveal` 输出可直接粘回 `.env` 的明文，
`--out FILE` 落盘并警告）、桌面端设置页「显示已保存的密钥」（新增 bridge 方法
`reveal_api_keys`）、`finesub doctor` 新增 `env-keys` 状态行。换机、重装 Windows
之前先导出。

过渡开关 `FINESUB_ENV_PROTECT=0` 暂停自动加密（解密照常、迁移保持未完成），给
仍直接读 `.env` 的旧代码（未收敛的 worktree）留缓冲；收敛后移除变量即自动转换。

配套：`finesub_bootstrap/secrets.py` 成为全项目唯一的 `.env` 解析/写入层（
`llm_runtime` 与桌面 SettingsStore 的两个自建解析器删除；桌面写侧从「白名单整文件
重写」改为逐行保留式更新）；`.env-sample` 删去死配置 `EXA_POOL`/`TAVILY_POOL`；
`.gitignore` 补 `.env.*`。

CLI 与桌面从这一版起**共用一个版本号、一个 tag、一个 GitHub Release**（由
`test_the_cli_and_the_desktop_app_ship_one_version_number` 强制）。`v0.3.0` 是这条
契约成立之前发的 CLI-only release，所以联合发布线从 0.3.1 起。

### 数据布局：一个用户一份数据

三种安装形式（桌面安装版、便携版、pip 安装的 CLI）的**个人数据统一到
`%LOCALAPPDATA%\FineSub\user-data`**——此前便携版把它放在包内，于是同一个用户可能有三个知识库，
还不知道应用在读哪一个。大文件（`models`/`cache`/`tasks`）默认仍在安装目录下，可以用
`finesub relocate <目录>` 整体搬到别的盘，也可以让两个安装指向同一处、不重复下载几个 GB；
位置记在数据根的 `locations.json`，找不到就自动回落，不报错。用资源管理器整个搬走安装目录能被
自动认出来；只搬大文件目录的话，到新位置双击里面的 `register-location.cmd` 即可。

`runtime` 永远留在安装目录下：它与版本绑定，而且 uv 在同盘时用**硬链接**把 wheel 从缓存链进
环境（实测 `torch_cpu.dll` 一份数据两个路径），把两者分到不同磁盘会让硬链接退化成复制、
总占用**反而多出约 5 GB**。装环境前会比较两者所在磁盘并在不同磁盘时告警。同理，单独清
`cache\uv` 几乎不释放空间——先删运行环境再清缓存才有效。

`tasks` 从 `user-data` 里挪了出去（每任务几十 MB、无上限增长），并进了更新器的保留名单；
任务历史改存相对路径，所以手工搬动文件夹之后"打开文件夹"不再全部失效。
卸载改成三档：默认删可再生的运行环境/模型/缓存，成品字幕与个人数据分别要
`--purge-tasks`、`--purge-user-data`；大文件目录一旦搬走或共用，默认也不删（另一个安装多半还在用）。

**从仓库源码运行仍然用仓库自己的数据**（`knowledge/`、`.env`、`.state`），
`FINESUB_CHECKOUT_DATA=0` 可退出；git worktree 解析到主仓，且 worktree 内的知识库自动更新默认
跳过，需要 `FINESUB_KNOWLEDGE_WRITE=1` 才写。

### 修复：并发写坏共享数据的三处

个人数据共享之后，几个此前不可能发生的竞争变成了可能，一并补上跨进程锁：数据迁移
（锚在 user-data **外面**，因为迁移要搬的正是它）、知识库 auto-apply（锚在
`<knowledge_root>.lock`，即知识库目录的兄弟文件——放里面会被 auto-commit 收编，锚在安装根则三个
前端锁的是三个不同文件）、以及共享下载缓存里同一个压缩包的并发下载（此前会互相写坏
`.part`，报出看起来像被投毒的 SHA-256 不匹配）。任务历史改成"锁 + 重读 + 按 id 合并"回写，
不再是内存快照整体覆盖。知识库拿不到锁的行为与"仓库脏"一致：跳过、保留提案、不推进 ledger、
只打 warning。

### 修复：装完 2.8 GB 依赖后倒在最后一步改名

有用户在装 Python 运行环境时拿到 `[WinError 5] 拒绝访问`：依赖全部装完、校验也过了，
只差把 `runtime/python.staging` 改名就位。Windows 上目录改名在树内还有句柄时会被拒
（刚写完的数 GB 文件正被杀软或网盘同步扫描），目标名被占用时也是同一个错误——
`MOVEFILE_REPLACE_EXISTING` 对目录无效。改名现在带退避重试；目标名的判定改用
`os.path.lexists`（`Path.exists()` 会跟随链接，把指向别处的 junction 当成不存在）；
清理旧目录时链接只删链接本身，不再递归进它指向的目录。仍然失败时报错说明是占用并给出
处置建议，而不是抛原始 `WinError 5`；且**保留已装好的 staging**——它已通过校验，重试
只需再做一次改名，不必重装数 GB。

### 修复：知识库被写进应用目录，会被下次更新删掉

桌面端装不上时，用户会直接用包内解释器跑 pipeline。这条路绕开启动器注入的
`FINESUB_KNOWLEDGE_ROOT`，而发行包的 `app/versions/<版本>` 同样带 `pyproject.toml` +
`src/asr_playground`，于是被当成源码 checkout，知识库落在了 `app/versions/<版本>/knowledge`。
更新器的保留名单只有 `user-data`/`models`/`runtime`/`cache`，`app/` 整体替换——数据会静默消失。
checkout 探测现在显式排除这种布局，并按安装布局反推数据根（安装版走
`%LOCALAPPDATA%\FineSub\user-data`、便携版走包内 `user-data`），与启动器注入的位置一致；
`.env`、`config.toml`、限流状态文件同理。已经写错位置的那份由新的数据迁移搬回来。

### 桌面包自带命令行

包根新增 `finesub.cmd` + `finesub.py`，与 pip 安装的 `finesub` **同源子命令**
（实现下沉到 `finesub_bootstrap/shell.py`，两个前端共用），直接驱动它所在的那份安装。
用户和 agent 不必再自己拼 `runtime\python -m asr_playground.pipeline`——那正是上一条的
成因。它不负责装资源：自己就跑在托管运行时上，缺资源时指回应用内的资源面板。

### 用户数据迁移机制

新增 `finesub_bootstrap/migrations/`：按 id 记账（`user-data/.migrations.json`）而非版本区间，
因为桌面与 CLI 共享同一棵 `user-data` 且各自跳版本。启动器与命令行都会在读用户数据之前跑一次；
失败只记日志、下次重试，绝不影响启动。首个迁移把 `app/versions/*/knowledge` 搬进
`user-data/knowledge`；两边都有知识库时不自动合并，持续告警直到人工处理。

### 首次任务提示

首次任务要按需下载模型权重（合计约 3.4 GB）并预热分离器的编译路径，这些都发生在任何
进度出现之前，不说明就像卡住了。新建任务页（选好输入后）与处理页各提示一次。判据是
「没有已完成的任务」而非「历史为空」——只失败过的机器同样还没缓存任何权重。

### 修复：安装 Python 环境时界面假死

`status()` 里的运行时体检会**同步**起一个 Python 去 import torch + 整条解码链——
实测 **14.7 秒**（热缓存，冷启动更久）。而它跑在 pywebview 绘制窗口的那个线程上，
`get_bootstrap_state` 和每次资源轮询都会调。表现就是"点安装卡住、点暂停后又显示成功"
（那次点击触发了新一轮，此时探针刚好返回）。是上一轮把 `REQUIRED_RUNTIME_IMPORTS`
从 4 个扩到 8 个引入的。

`install()` 本来就在写 marker 之前验证过，而 marker 绑定 lock 哈希——所以 `status()`
再跑一遍是在 UI 线程上重复证明已证之事。改为纯文件系统检查（site-packages 下的包目录
是否还在，CT2 的补丁版从 dist-info 目录名读），进程零开销；导入探针保留在安装时。

### 模型优先复用本机已有缓存

`FINESUB_MODEL_DIR` 一旦设置就完全接管，从不看常见缓存目录，于是已经下过的权重被再下
一遍。改为"先找再下"（`finesub_bootstrap/model_caches.py`），两种粒度：

- **分离器**：精确到文件——检查 `~/.cache/audio-separator` 里有没有那个 ckpt。
- **Hugging Face**：只有一个内容寻址的缓存根、无法搜索多个，所以是**按缓存整体**判断：
  常规根里已有本管线用到的任一仓库就整体复用（包括之后新下的）。里面若只有别人的模型
  则不动它——往别人的缓存里下载既意外、卸载也清不掉。显式设了 `HF_HOME` 一律不猜。

编译加速产物（accel）**不跟着走**：它绑定单一 torch 构建与 GPU，写进共享缓存会留下
无人能归属、也无人能清理的文件。为此新增 `managed_separator_model_dir()`。

### 修复：拖拽时的卡顿

`dragenter`/`dragover` 的 Python 回调是空的，只为 `preventDefault` 而存在——而
pywebview 生成的监听器同步执行 `preventDefault` 后，仍会把整个 DragEvent（含
dataTransfer）序列化过桥调用它，`dragover` 每秒几十次。加 `debounce=500` 压掉这些无用
往返；`preventDefault` 不受影响，drop 仍然即时。

### 修复：LLM 阶段因缺 tzdata 直接崩溃

`llm/rate_limit.py` 在**模块导入时**构造 `ZoneInfo("America/Los_Angeles")`（对齐 Gemini
的日配额窗口），而 Windows 的 Python 不自带 tz 数据库。`tzdata` 从来没有出现在
`pyproject.toml` 里——只在 `.github/workflows/ci.yml` 有一行临时 `pip install`，那正是
同一个 bug 的补丁。任何按 extras 安装的环境（包括桌面托管运行时）一进 LLM 阶段就
`ZoneInfoNotFoundError`。已加进 `[harness]`（限 Windows）并重编 lock；CI 的临时行删除。

### 任务目录与日志

- 任务 id 由裸 uuid 改为 `<stem>-YYMMDD-HHMM-<6位hex>`，`user-data/tasks` 下终于能看出
  哪个目录对应哪次任务。stem 取自输出名称，没设则取源文件名。
- 任务结束后（成功/失败/取消）自动把日志写到该任务目录的 `task-log.txt`——日志抽屉
  是有上限的环形缓冲且随应用关闭消失，而值得上报的失败往往过后才被注意到。
- 「复制日志」改为「导出日志」（下载为文件），历史页右上角新增「打开任务目录」。

### 桌面任务表单调整

- 输出结果收敛为两项：原始字幕（无 LLM 处理）/ 最终字幕（须 LLM 处理）。
- 补充信息移到基础设置并给出实例文案；处理设备移入高级设置。
- 输出名称去掉常驻说明，改为**填错才提示**（与 `TaskRequest.validate_name` 同规则）。
- 知识库默认改为「自动更新」。配套修正一处会误伤的门禁：
  `required_capabilities` 现在要求 stage 真的进到 LLM 才需要 git——否则默认设置下
  每个纯转写任务都会被要求下载一个根本不会运行的 git。
- 运行日志显式可选中，并新增「复制日志」按钮。
- 资源磁盘估算修正：`uv` 那一栏此前标 24.5MB（uv 二进制），但安装它会拉取整个
  `pylock.win-py312.toml`——**实测 torch 一个就 2.56 GiB，合计约 2.83 GiB**，低估了两个
  数量级。另在提示里补上模型权重的按需下载估算（约 3.4 GB），并把「模型如何管理」
  的说明改为逐个列出权重与体积。

### 桌面支持 URL 输入

- DropZone 增加链接输入（文件选择 / 拖放 / 粘贴链接三选一）。管线一直支持 URL，
  桌面此前单方面砍掉了这个入口。链接与文件走同一条状态路径，yt-dlp 按需拉取。
- 前端的 URL 判定 `isUrlSource` 与后端 `finesub_bootstrap.capabilities.is_url`
  规则一致——不一致就会出现"UI 收下了、后端拒绝"的输入。

### 外部工具改为托管资源（git / yt-dlp），并复用系统已有的

- **manifest 新增 git（MinGit）与 yt-dlp（PyPI wheel）**，`ResourceManager` 一行未改。
  不进 lock：运行时 marker 含 lock 哈希，改 lock 会触发数 GB 的环境重建，而改 manifest
  不碰运行时。注入方式按性质分——git 走 PATH，yt-dlp 走 **PYTHONPATH**（管线是
  `import yt_dlp`，不是调可执行文件）。
- **懒装**：git 只在 `--knowledge update` 时装，yt-dlp 只在 URL 输入时装。规则收在
  `finesub_bootstrap/capabilities.py`，桌面（读 TaskRequest）与 CLI（读命令行）共用，
  避免两个入口对"这次运行需要什么"产生分歧。
- **复用系统已有的 ffmpeg / git**：照 `_find_system_python` 的模式——`which` 找到后
  实际执行校验（ffmpeg 还要查必需编解码器，缺了就退回托管副本，否则会在管线中段才炸）。
  一台已有 ffmpeg 的机器因此省掉 146MB。探测可注入，否则测试结果会取决于跑测试的机器。
  yt-dlp 无法这样复用：托管解释器看不见用户的 site-packages。
- `task_ready` 改为按请求校验，错误信息说明缺的是哪个工具（此前无论缺什么都说
  "请先安装 Python 运行环境和 FFmpeg"）。
- CLI 从 manifest 取全部资源（此前硬编码只取 ffmpeg），`doctor` 统一报告三者状态与来源。
- run metadata 新增 `tools.ffmpeg`：复用系统版本让行为依赖用户机器，路径与版本要可追溯。
- `ResourceStatus` 新增 `optional`：按需工具在资源面板里列出（否则"缺 git"的报错会把
  用户指向一个找不到 git 的面板），但不计入就绪数与所需空间，缺失时也不显示告警图标。

### 知识库：git 缺失不再让任务失败

- **`_run_git` 不再抛裸异常。** 本项目不安装 git，所以「没有 git」是常态；此前
  `subprocess.run(["git", ...])` 会抛 `FileNotFoundError`，在字幕已经落盘之后把整个
  任务带崩。改为返回 `returncode=127` 的合成结果，调用方现有的「非零即失败」处理原样生效。
- **前置拦截，不浪费配额。** `run_knowledge_update` 在 `execute and apply` 时先查 git，
  不可用就返回 `skipped: "git_unavailable"`，**一次 API 调用都不发**（此前要先花钱生成提案、
  走到 apply 才发现装不了）。
- **三条失败路径统一降级为 warning，且都不推进 chunk ledger**，所以修好后重跑会完整重做：
  git 缺失 / 仓库脏或在别的分支 / 文件已改但 commit 失败。最后一条原先在改成 warning 后
  会继续走到 `_append_chunk_ledger`——等于把「没提交」记成「已完成」，那批改动将永远不被记录；
  现已阻止。

### 桌面任务控件

- 新增 `name`（对齐 CLI 的 `--name`，产出 `out/<name>/<name>.srt`，带路径分隔符校验）、
  `extra_info`、`knowledge`（暂只暴露 none/update）、`cleanup_intermediate` 四个控件；
  移除联网检索开关（保持默认开启）。
- **中间产物默认不再清理。** 此前无条件删除，连 `stable.json` 和整个 LLM artifact 目录
  一起删——后果是重跑要从头做分离与识别，纠错翻译也没了输入和 checkpoint。现在改为可选，
  且**即便勾选也始终保留 `stable.json` 与 artifact 目录**：它们相对人声音频很小，却决定了
  重跑是否廉价。

### 桌面自动更新（此前从未跑通）

- **更新检查不再打 `/releases/latest`。** 那是仓库级的，而本仓库还发 CLI 快照与
  patched CT2 wheel——发完 wheel 之后 "latest" 就指向它，更新检查必然抛
  "missing the signed update manifest"。改为列举 releases、取最新一个**真正带签名
  manifest** 的（`is_desktop_release()`），签名与 tag 校验仍在其后兜底。顺带覆盖
  资产分批上传期间的半成品 release。
- **签名发布流程从未被执行过**：v0.2.7 只发了 portable zip，`build_release.py`
  产出的 `update-manifest.json` / `.sig` 一次都没上传。`desktop/README.md` 新增
  发布 runbook，说明四个资产缺一不可。
- `build-release.ps1` 的陈旧默认值（`-SupportedFrom 0.2.3`，一个从未发过签名
  manifest 的版本）改为空 = 所有旧版本拿 full 包，增量必须显式声明。
- **应用内一键更新接线完成。** 新增 `install_update` / `get_update_install` 两个 bridge
  方法与 `UpdateInstallManager`：安装跑在后台线程，前端轮询进度快照（与运行时资源下载
  同一套形状）。bridge 调用必须立即返回——pywebview 在绘制窗口的线程上派发它们，而 full
  包是几百 MB。设置页显示下载进度，完成后按 app/full 分别提示"重启"或"退出以完成更新"。
- **独立 updater 恢复构建。** `desktop/FineSubUpdater.py` 一直在仓库里，但
  `build-bootstrap.ps1` 不构建它——而 `_install_full` 要求
  `<root>/updater/FineSub Desktop Updater.exe` 存在，否则抛 "Installed updater runtime
  is missing"。也就是说 full 更新在任何真实安装上都不可能成功，而按上面的策略 0.2.7 →
  0.3.1 恰恰只能走 full。已补回第二个 PyInstaller 目标并实跑构建验证。
- **updater 失败不再挂住进程。** 它是 windowed 构建（无控制台），未捕获异常会变成
  PyInstaller 的模态 traceback 弹窗——而此时 FineSub 已退出，没人会去点。实测确认：修复前
  一个坏请求会让进程一直活着。现在兜住异常、写 `<request>.error.txt`、以 1 退出。

### 托管运行时

- **lock 重建。** `desktop/runtime/pylock.win-py312.toml` 上次对齐是
  2026-07-31，`[asr]` 还是 whisper-timestamped 时代；fw-refine 迁移之后它**一个解码器
  都不含**（faster-whisper / ctranslate2 / transformers / silero-vad / triton-windows
  全缺），装得上、跑不了。现按今天的 `[asr]+[harness]+[desktop-worker]` 重新生成：
  torch 2.8→2.11.0+cu128，包数 75→88。
- **补丁版 CTranslate2 进 lock。** `[desktop-worker]` 用 direct reference 锁到 release
  wheel（带 sha256）。桌面运行时只有 win_amd64/cp312/cu128 一个组合，所以 `[asr]` 那边
  规避的平台钉死在这里是零成本的。开发机与端用户安装都走同一份 lock，两边自动拿到
  补丁版。
- **运行时校验补全。** `REQUIRED_RUNTIME_IMPORTS` 原本只查分离器一侧，加入
  `faster_whisper` / `ctranslate2` / `silero_vad` / `transformers`，并校验 CT2 的
  `__version__` 带 `wtrefine`——原版能 import、能满足 `==4.8.1`，只是跑不了 fw-refine。
  探针脚本抽成 `runtime_probe_source()` 以便直接测试。
- **lock 漂移进默认测试。** `desktop/scripts/tests` 加入根 `testpaths`：契约由仓库根的
  `pyproject.toml` 打破，而 desktop CI 只在 `main` 上跑。断言从"torch 版本对不对"改为
  "三个 extra 的每个直接依赖都在 lock 里且版本相容"，拿旧 lock 验过会报 10 条。
- 桌面任务恒开 `vad_silero_assist`（CLI 仍是 opt-in）：桌面任务必经分离器，而流式化后
  它的边际成本约 1s。
- 后台控制台隐藏、真实系统托盘、角色主题与 Yanami 主题、深色完成页、纯字幕产物发布、
  本地视频先转码再 ASR、分离器/RoFormer 运行时依赖校验、实时日志跟随（PR #7）。
- 清理：`setup-dev.ps1` 的 `-IncludePipeline` 已成空开关，删除；
  `media/source.py` 的 `ensure_aac_audio` 死别名删除；测试里 8 处
  `whisper_timestamped` 桩模块 monkeypatch 与 `HEAVY_IMPORTS` 条目删除（生产代码早已
  零引用）。

## [0.3.0] - 2026-08-05

### 移除

- **`whisper-timestamped` backend 整个移除**，`fw-refine` 成为唯一 ASR backend。
  同时删除：`--asr-backend` 开关（pipeline 与 vad-asr）、`asr-wt` 命令、`WtModelPool`、
  `naive_approach` 退避梯子、`asr_transcribe_seed` / `whisper_timestamped_mode` metadata
  （fw-refine 从不读 seed，那条记录一直是假的），以及 `whisper-timestamped`、`dtw-python`
  两个依赖。checkpoint fingerprint 的 `asr_backend` 字段随之删除（只剩一个 backend）。
  理由：为优化要改 refine 内部逻辑，维持两套行为对齐的成本不划算；迁移验收（5 素材 /
  50.6 分钟）显示 fw-refine 快 3.19×、内容量差 ≤1%、救援活动在每个素材上都更少。
  回溯点：`dev` 的 `1fcc4e1`。
  **`asr-refine` extra 因此从可选变为必需**。

### 新增

- **`--qwen-verify {auto,on,off}`（默认 auto）：第二模型校验证据。** Whisper 池释放后加载
  Qwen3-ASR-0.6B，对三类嫌疑段（整段收尾套话、CJK 主导 run 里的 Latin 段、噪声腿将丢弃的段）
  重认，证据写进段级 `qwen_verify`，决策留给 stabilize。`[asr]` 因此新增
  `transformers>=5.13,<6`（pip 增量约 100MB，模型首次运行下载约 1.5GB 至 HF 缓存）。
  并入 `[asr]` 而非可选 extra，是为了让同一条命令在任何安装上产出相同的 stable。
- **词首修正**（`speech/recognition/word_starts.py`）：`[*]` disfluency 块按能量门决定
  融合/删除，再对首词做 VAD interval 与 pause_hint 锚点 clamp。gold 上词首 |err| 中位
  41→18ms。
- **LLM 窗口质量护栏 `max_window_subtitle_tokens`**（`ModelLimits` 默认 10,000，config.toml
  `[chunking]` 可覆盖，`0` 关闭）：单窗 `<asr_result>` 的 token 上限，独立于输出系数——
  窗口过长时翻译质量会掉，哪怕输出装得下。窗口数估算与真实 countTokens 校验两处生效，
  超限走既有的 k+1 重排；快速模式的 auto 判定也以它为第三道门（快速窗口就是全片，
  最容易撞上）。
- `energy.WaveformObserver` 钩子：让第二个信号搭车读取 VAD 已经算好的归一化 block，
  不必再解码/重采样/归一化一轮。不传时 energy 模块的每个输出值逐字节不变。
- `fw-refine` 的 multi-audio batch 设计与本机实测落入 `docs/wt-refine-port.md`：CT2 的
  `real_audio_frames` 改为逐样本、split-encode 批模式、确定性契约，以及模型 × beam × GPU profile
  的 batch size 档位表。迁移本身值 6.3×、batch 再叠 1.8×，故 batch 不阻塞 P0。
- 新增显式 opt-in 的 patched CT2 `fw-refine` checkpoint：greedy/beam=5 以 1-pass
  winner trace 对齐 WT refine，默认收集低成本 path 信号；修复后的 disfluency 保持显式
  开关，启用后同样以 `alignment_events` 透传到 aligned/stable 产物。FineSub 暂不消费。
- `fw_refine_backend.transcribe_batch()`：一次批量解码若干 ≤30s 窗口。split-encode（逐窗口
  encode、只批 decoder），再把每条结果回放进普通 `transcribe()`，因此 segment/词/事件的组装
  只有一份实现。24 个真实生产窗口上文本一致 22/24（18 条连词级时间逐位相同），加速 1.73×。
  调用方的组批策略尚未实现。

### 变更

- **VAD 归一化的峰值限幅由全局缩放改为逐样本 clamp——会改变 VAD 输出。** 旧实现是
  `x * (0.98 / 全局峰值)`，让**单个最响样本**决定施加到整轨的位移：kaguya60 的 0.00029%
  样本把 60 分钟压低 2.18 dB，mia 的 0.00004%（约 25 个样本）把 108 分钟压低 4.12 dB；
  7 条真实分离人声里 4 条触发。而 VAD 判据里有一批绝对 dBFS 阈值，它们能跨文件通用正是
  靠上一步把局部 RMS 对到 −24 dBFS，全局缩放随后又按文件拆掉一部分标定。改为削掉越界样本
  后，weighted 轨上受影响帧从「全部有信号帧」降到 179/4292，越阈帧从约 2000/20000 降到
  0/1。**限幅曾触发的文件重跑会得到不同 segment**（kaguya60 752→753、语音 +4.8s；
  mia 2561→2522、+46.1s），未触发的文件逐字节不变；旧产物重跑即可。同时也删掉了流式
  路径中专为施加全局标量而存在的第二遍 pass。详见 `docs/vad-energy.md`。
- **ASR 固定单 worker，移除单文件分片设计**。`sharding.py`、`ResourceProfile.wt_instances`、
  `--wt-workers`（pipeline 与 vad-asr 两处）、interval ownership 标记与合并、shard partial
  一并删除；GPU profile 现在只决定人声分离实例数，metadata 不再记录 asr workers。
  实测 worker=3 相对 worker=1 在 wt 上仅 1.40×、fw-refine 上仅 1.20×，代价是显存 2.4→6.5 GB；
  换到 fw-refine 后 ASR 已非瓶颈（人声分离占语音段 72%）。回溯点：`dev` 的 `1fcc4e1`。
- **ASR 分组改为按合成后的音频长度规划**：组尾垫料（至多 0.7 秒原始音频 + `--gap` 秒静音）
  此前不计入分组长度，于是按 30 秒规划的组实际可达 31 秒、溢出编码窗口。11 个真实 clip 上
  超窗分组 102 → 56，总组数 388 → 405。**这会改变分组边界，因而改变 ASR 输出**（11 个 clip
  中 9 个分组不同）；旧产物不会自动失效，需要重跑才能得到新分组。`combined_group_duration()`
  语义不变（auto language 短组启发式仍按「说了多少话」判断）。
- `asr-pipeline` 新增开发用 `--asr-backend {wt,fw-refine}`（默认 `wt`，与 `vad-asr` 一致）。
  面向一般用户的文档不介绍该开关。
- ASR checkpoint fingerprint 新增 `asr_backend` 且**无默认值**：两个 backend 对同一段音频给出
  不同词级时间，中断后换 backend 续跑此前会静默复用另一侧的 partial，把两种输出缝进同一份产物。
- `faster-whisper` 与 `ctranslate2` 移出 `asr`，独立为 **`asr-refine`** extra 并精确钉版
  （1.2.1 / 4.8.1，均为当前最新且互相兼容）。fw-refine 不在 `asr-pipeline` 的可达路径上，
  普通用户不必装一份链了 CUDA 的 CTranslate2；同时也消除了 `asr` 与 desktop runtime lock
  之间「lock 缺这两个包」的静默不一致。fw-refine 继承 faster-whisper 内部实现并读取 CT2
  解码轨迹，小版本变动可能悄然改变输出；升级顺序固定为先 faster-whisper 后 CT2——CT2 的
  可选范围由 fw 声明的 `>=4.0,<5` 决定。

### 性能

- **`--vad-silero-assist` 从「比 VAD 本身还贵 4 倍」降到几乎免费**：60 分钟分离人声上
  该 opt-in 此前在 17 秒的 VAD 阶段之上再花 72 秒，现在 CPU 约 3 秒、CUDA 约 1 秒。
  silero 的逐帧 JIT 调用（每 32ms 一次，一小时 112,500 次派发）改为：帧间独立的
  STFT+encoder 整批计算，LSTMCell 的权重驱动全序列 `nn.LSTM`；概率再搭车在 VAD 自己的
  流式 block 上算，省掉第二遍解码与整段波形常驻（0.23 GB/小时）。与逐帧实现的最大概率
  差 1.4e-05，判据阈值零翻转。CUDA 路径显式关闭 TF32（开启会让 112,500 帧中 16 帧越过
  `CAP_SIL_THR`）。

### 修复

- `fw-refine` 在 CTranslate2 缺少该设备的矩阵后端时，给出指向构建要求的可读错误，而不是从库
  深处抛出的 `No SGEMM backend on CPU`。该情况无法从 `get_supported_compute_types()` 查出
  （只有 CUDA 后端的构建对 CPU 仍报 `float32`），只能在首次 encode 处补上下文。
- `fw-refine` 的词切分不再丢弃永远无法解析的 `U+FFFD` token。真实幻觉撞上解码上限时会在半个
  字符处截断，旧实现一直等待补全、循环结束时静默丢弃该 token，使词分组与 one-pass 解码轨迹错位并
  抛 `ValueError`；310 个真实生产窗口里有 6 次触发。同时补上 fw-refine 的退避链——任何失败改退到
  该后端自己的 teacher-force 对齐（`naive_approach` 是 whisper-timestamped 的选项，对 fw-refine 会
  原样重放同一次调用），两次都失败才丢弃该 group，不再终止整个 run。
- recall 临时组的 complement 切片现在继承源 interval 的 shard 归属。此前它新建裸 dict，
  分片合并时以「missing interval ownership」中止**整个 run**；触发条件是 workers ≥ 2 且
  某个 block 命中 recall（≥5s 未覆盖），与 ASR backend 无关。
- Desktop 的 API Key 设置改用现行 CLI provider pool 变量，Windows AI runtime
  lock 重新对齐 Torch/Torchaudio 2.8；新增跨 Desktop/CLI 契约测试，避免两侧再次漂移。
- Desktop 完成页只展示实际存在的产物并暴露 run metadata；WebView 重载会恢复仍在
  运行的任务，翻译模式明确提示 Gemini 媒体片段上传。
- Desktop 默认 LLM level 和后处理 profile 范围与生产 pipeline 对齐；新增独立
  `desktop/VERSION` 作为桌面发布版本的单一来源。

## [0.2.0] - 2026-07-30

### 新增

- ASR 新增单文件 Whisper Timestamped 分片、分组 checkpoint、运行时 metadata、
  GPU stage gate、stall watchdog 与资源用量记录；4/8/12/16GB profile 会在文件内部
  分配 separator 和 WT 并发。
- 字幕分句改为全局 DP，并新增分割点金标准、标注工具和系统化评测资料。
- LLM harness 新增可配置 API key pool、sticky retry 后的组合冷却，以及更完整的
  token budget、任务报告和搜索证据处理。
- 新增 `config.example.toml`、桌面 launcher 资源配置和 Windows token counter 更新。

### 变更

- 生产代码重组到 `asr_playground` namespace，明确 media、speech、subtitles 和
  workflows 边界；命令行入口和打包清单同步迁移。
- Batch 从文件级 ASR 并发改为单文件独占 profile、文件内分片并发，避免两层并发相乘。
- 桌面应用同步外观控制与 UI 刷新，并在构建 bootstrap 时优先使用 conda env-root Python。
- OpenCC 转简加载器与本地 token counter 改为跨调用复用；ASR 模型从共享 checkpoint
  直接构建 FP16 实例，降低重复加载开销。

### 修复

- 修复 wheel 漏装 `batch`、`gpu_stage_gate`、`run_metadata`、`segment_split` 和
  `wt_shard` 顶层模块，以及 license metadata 无法在声明的 setuptools 下限构建的问题；
  增加顶层源码与 packaging 清单一致性测试。
- ASR checkpoint schema 升至 v2；旧 partial 明确失效并从头重跑，sharded merge
  遇到缺失 interval ownership 的结果会显式报错，不再静默丢字幕。
- `segment_split` 对有文本但没有 word timestamps 的 segment 合成一条带来源标记的
  segment-span word；无法安全归一化时保留原输入，不再从全局 DP 输出中消失。
- Reference ingest 在迁移到统一 batch workflow 前，先与普通 batch 一样固定每任务
  `wt_workers=1`，避免文件级并发与 shard 并发相乘。
- Pipeline 的 LLM round 汇总和 task report 现在遵循显式 `task_artifact_dir`；batch
  同一 logical run 的后续 pass 会继承已执行 stage，stage metadata 不再混出
  `reused` 加旧 `elapsed_sec` 的矛盾记录。
- 字幕渲染在时间轴后处理前修复 cue 重叠，保证不丢文字。
- 修复 separator dotted 临时文件识别、Gemini key 全部跳过时的错误类型，以及
  flash gap/end pad、RPM 失败计数等重试与边界问题。

### 变更

- `--wt-workers` 明确为开发/不安全 benchmark 覆盖参数；生产调度继续由 GPU profile
  和 batch runner 决定。
- 补齐 run metadata、WT sharding、segment split 和 packaging 测试的 pytest 域 marker。

## [0.1.1] - 2026-07-27

Prompt version: `zh-subtitle-correction-csv-v65`。

### 新增

- 新增 Windows Desktop 应用，提供任务管理、资源管理、运行时设置与日志查看等桌面工作流。
- Pipeline 新增 `--name`；视频任务默认使用高质量多模态模型，并在不可用时自动降级。

### 变更

- Prompt 升级到 `zh-subtitle-correction-csv-v65`：删除 CapableA、BasicC 及 BasicC 的 JSONL 输出支线，现行变体为 capableB/C + basicA/B。
- 单窗口 query、correction 与 fast round 1 的目标字幕序号每窗重置为 `1..N`，只读前文按时间顺序编号为 `1-M..0`；harness 校验后映射回稳定源序号，oneshot、replay、benchmark 与任务反馈同步采用该契约。
- 改进搜索证据包和研究阶段，减少冗余上下文并提高可用证据密度。
- 完善 ASR 语言历史、分组 checkpoint 与 Whisper fallback，扩展救援阶梯和尾段回交策略。
- ASR 依赖更新至 PyTorch 2.8 系列。

### 修复

- Pipeline 与最终 SRT 输出改为原子写入，避免中断时留下不完整文件。
- 修复 clip 预取线程安全问题，并补充 Pipeline 失败诊断信息。

## [0.1.0] - 2026-07-23

首个 beta 版本。Prompt version: `zh-subtitle-correction-csv-v63`。

### 功能

- 本地长音频转字幕完整流水线：人声分离 → VAD + Whisper ASR 对齐 → ASR 稳定化 → SRT 输出
- 实验性 LLM 纠错与翻译后处理（Gemini），支持 6 档 preset（route × level）、fast 模式、多轮搜索调查
- 批量运行（三阶段流水线并行，单项失败隔离，断点续跑）
- 本地知识库（自动采集/统一更新/精修对照）
- URL 输入支持（yt-dlp 下载 + 自动 ID 映射）
- 词级字幕输出
- SRT 后处理（繁简转换、短轴延长、标点清理）
- GPU 显存档位（8/12/16GB）自适应
- 流式 VAD 与流式 ASR 对齐（内存上界恒定，支持任意时长音频）
- LLM session resume + 纠错窗口中途 resume
- Prompt 变体系统（capableA/B/C + basicA/B/C）

### 依赖

- Python >= 3.12
- 核心流水线：torch~=2.9.0, torchaudio~=2.9.0, numpy, soundfile, whisper-timestamped, onnxruntime, audio-separator, numba
- LLM harness：httpx, yt-dlp, opencc-python-reimplemented
- 开发：pytest, pytest-xdist, sudachipy, sudachidict_core

### 已知限制

- LLM 纠错翻译层为实验性功能，默认不包含在生产 stage 中（需 `--stage translated-srt` 或 `final-srt` 显式启用）
- 仅提供 Windows 预编译 token counter 二进制；Linux/macOS 回退到免费 countTokens API 或启发式
- 无 GPU 时回退 CPU（速度显著下降）
