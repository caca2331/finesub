# default_style
示例：一家字幕组的翻译口味（骨架，正文可全部替换）

## 档案

<!-- 用途：这套风格是谁的、对什么素材成立。
     不收：具体怎么做——那是「约定」。
     行式：可选 [标记] + 一句话内容。标记不是必须的，无标记行同样合法。
     标记（缺了完整预览会留一个空槽）：
       [本名] 这套风格的名字，也就是 `--style` 点名时写的那个串；通常是字幕组名或译者名
       [作者] 谁的口味。自由文本，只给人看——style 没有真相，作者归属也不需要可验证
       [适用范围] 对谁成立（该组全部投稿 / 某主播 / 某题材）。⚠ 静态选取下它**不参与任何判断**，是给人看的说明，别拿它做匹配 -->
- [本名] default_style
- [作者] 某字幕组
- [适用范围] 该组全部投稿

## 约定

<!-- 用途：一句祈使式的做法，一行一条——**唯一会被当作规则读的一节**。每条带一个 `[标记]` 作短名，正例/反例用同一个标记指回来。条数与单条长度都有上限，满了要加新的就得退掉旧的或把两条并成一条。
     不收：理由与解释（一行写不下，说明这条该拆成两条）；某个专名怎么译（那是对应词条的术语行，不是风格）；「翻译要准确」这类对谁都成立、不带这套口味的话。
     行式：可选 [标记] + 一句话内容。标记不是必须的，无标记行同样合法。
     上限：最多 10 行、每行 ≤ 200 字（含 [标记]）。满了还要加，就得先退掉一条、或把两条并成一条——写入时会校验，超了整批拒绝。 -->
- [自称转换] 说话者用自己名字/昵称自指时，译文改用第一人称「我」

## 正例

<!-- 用途：照着约定做出来的译文，`[标记] 原文 → 译文` 一行一例，标记就是它演示的那条约定。箭头两边只应差在这条约定上：顺手带进来的无关改动（专名定名、别的约定）先改掉，否则这一例同时演示了两件事。举例是为了让约定不被误读，不是为了穷举——一条约定至多一个正例（标记在节内唯一）。
     不收：带标记却在「约定」里找不到那条规则的孤例——它证明不了任何东西，只会占注入面（无标记的一般性例子不算孤例）。
     行式：可选 [标记] + 一句话内容。标记不是必须的，无标记行同样合法。
     上限：最多 10 行、每行 ≤ 200 字（含 [标记]）。满了还要加，就得先退掉一条、或把两条并成一条——写入时会校验，超了整批拒绝。 -->
- [自称转换] Nono酱小时候还被说是大总统呢 → 我小时候可是被夸过是「大总统」呢

## 反例

<!-- 用途：违反约定的译法，同样 `[标记] 原文 → 译文`。⚠ 箭头右边必须是**那个不该出现的译文本身**，不是「为什么不行」的说明——两边都是译文，这一节才和正例可比；理由要写就补在行尾括号里。一条约定至多一个反例。
     不收：与风格无关的错译（歪曲原意、丢信息）——那是纠错的事；精修与机器译文的差异本身也不等于错，见 `docs/knowledge.md`「精修字幕怎么读」。
     行式：可选 [标记] + 一句话内容。标记不是必须的，无标记行同样合法。
     上限：最多 10 行、每行 ≤ 200 字（含 [标记]）。满了还要加，就得先退掉一条、或把两条并成一条——写入时会校验，超了整批拒绝。 -->
- [自称转换] Nono酱小时候还被说是大总统呢 → Nono酱小时候还被说是大总统呢

## 元数据

最近更新日期: 2026-09-02
